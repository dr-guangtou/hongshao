# Session Handover — 2026-07-16

## Goal
The deposition-primitive rethink (exp38) is COMPLETE and merged. This
session ran its full ladder (stages 0-3), adopted the new kernel, and
parked the core channel with a measured restart plan. **Next session (user
direction): revisit the "core" — the compact inner component.**

## Completed This Session (all merged to master `07a9d3a` and pushed)
1. **exp38 stage 0** — the three data diagnostics: the measured
   epoch-to-epoch added light has Sersic n = 2.1-3.1 wings at R50 =
   15-47 kpc, centrally peaked (the Gaussian's width rail = a missing
   tail); per-galaxy shapes nearly collapse in R/R_half coordinates
   across epochs (drift 0.03 vs 0.11-0.18 dex); shells rejected.
   (`stage0_diagnostics.py`, kernels saved for reuse.)
2. **Stage 1 shootout** (`stage1_shootout.py`): heavy-tailed deposits
   (power-law-tail "moffat", Gaussian-core + exponential wing) take the
   width scale off the bound at every epoch; the slope-sigmoid profile
   family has per-epoch capacity (0.6%) but non-interpolable raw
   parameters; the 2-param evolving-Re template closes best.
3. **Stage 2 promotion** (`stage2_multiepoch.py`, `stage2_family.py`):
   **ADOPTED (user): 1ch-mof — single-channel power-law-tail kernel,
   12 params, NO parameter at a bound** (rc0 235 kpc, gamma 1.38, peak
   z 3.4), held-out pinned shape 18.5-14.2% by epoch, best
   differential-deposition pass (0.39/0.12 vs data 0.37/0.11), flat
   outskirt terciles. 2ch-exp (16p) = the accuracy alternative (avg
   16.3%; its stress test found a FINITE wide-scale optimum at 3.19 —
   the exp35/36 rail dissolved). Template-z conditioned on
   [DiffMAH(4), c200c] reaches 16.3-18.8% (matches the kernel at z<=1).
4. **Stage 3 — the inner-mass deficit** (`stage3_inner.py`, user-raised;
   M(<5) -8 to -12% held-out): decomposed via provenance, re-aimed
   objective, retention floor, core channel, quenched core, and
   single-vs-joint fits. Verdict: mostly a POPULATION-SHARING limit
   (-5 to -8% in any shared single-epoch fit, ~0% per-galaxy) + a
   ~3-4-point multi-epoch transport tax (z=0.4-only fits set q = 0.00;
   the joint fit needs q ~ 0.9 which drains the core). The core channel
   (f_core = 0.19 at ~2 kpc) fixes the inner masses (M<5 -2.4% inner-aware,
   held-out shape avg 15.0% — below the statistical wall) but breaks the
   differential test (0.53/0.19) and the outskirt terciles — PARKED, not
   graduated (user decision). Quenched core = a measured null.
5. **The core post-mortem (the restart key)**: the core channel INHERITED
   the kernel's power-law tail — 41%/23%/8.3% of "core" mass beyond
   5/10/30 kpc; at f_core = 0.19 that is 1.6% of ALL stellar mass in the
   outskirts, matching the failed overshoot. An exponential core at the
   same R50 leaks 0.0% beyond 30 kpc.
6. Figures: 30+ (stage0 similarity/autopsy, stage1 tracks, stage2 QA
   sets for both variants, stage3_decision + 3 QA sets). Two lessons in
   doc/lessons.md; todo updated (exp38 complete + the PARKED core entry).

## In Progress (Not Finished) — the next session's task
**Revisit the core channel** (todo has the full plan). Three measured
leads, in order:
1. A dedicated STEEP-TAILED core form — a SHOOTOUT over several
   profiles, not just one (user direction): (a) a non-migrating GAUSSIAN
   core (no wings at all = zero outskirt leakage by construction; its
   kernel-role failure was missing wings, which is a virtue in the core
   role); (b) an exponential (Sersic n=1; 0.0% beyond 30 kpc at the
   fitted R50); (c) cuspier Sersic n = 2-4 (more mass inside 2-5 kpc
   per unit core mass, moderately steeper tail than the Moffat);
   (d) the inherited-Moffat baseline for reference. All closed forms
   exist in `shapes.py` (sersic_cog covers a-c: n=0.5 IS the Gaussian).
   Implementation: parameterize the core form in `stage3_inner.py
   model_cogs_core` (or a fresh exp39), then the standard fit + cv3 +
   physics3 round per form.
2. Condition f_core on the halo mass AT z=2 (`pop npz logmh_zk_real[:, 4]`;
   user suggestion — provenance shows the model core is built by z=2-4
   deposits), instead of the current z=0.4 logMh (cond[0]).
3. Per-galaxy core diversity beyond shared conditioning (the
   population-sharing limit carries most of the remaining deficit) —
   e.g. a per-galaxy core fraction treated statistically.
Judge by the SAME pre-registered criteria: differential curve
(0.37/0.11), outskirt terciles, no parameter at a bound, held-out shape
vs 18.5-14.2%, and the M(<5)/M(<10) bias table.

## Problems / Blockers
- None blocking. Watch-outs: (1) `export
  HONGSHAO_DATA_DIR=/Users/shuang/Desktop/tng300_mah_mprof` every run;
  (2) long fits via nohup + caffeinate -im + disown with a log tail;
  (3) exp38 scripts path-load exp35/exp36 modules — spawn-safe worker
  pattern (`_w_init`), penalties added parent-side; (4) 1ch-mof has a
  second basin at 4% better loss with g railed at 4.0 and degraded
  physics — always check `_at_bound` and the physics tests; (5) never
  commit doc/CLAUDE.md; commit THIS handover as the next branch's first
  commit; (6) usetex figure text via qa._tex/_pct; (7) the plain-language
  summary rule (AGENTS.md): every number defined in place.

## Key Decisions
- The Gaussian deposit is retired: the width rail was a missing
  power-law tail (measured, stage 0; dissolved by the stress test).
- 1ch-mof adopted over 2ch-exp: parsimony + bound-free + best physics;
  the two-channel split is unnecessary once the tail exists.
- Core channel parked (not graduated) despite fixing the inner masses:
  the physics tests are the kernel's reason to exist.
- The inner deficit accepted for the kernel (the statistical emulator
  is the observation-facing mass product at 1-2% bias).

## Branch State
- Branch: master, clean, pushed through `07a9d3a`
  (exp38-deposit-rethink merged and deleted).
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit).
- Gitignored outputs in place: exp38 `outputs/*.npz` (stage0 kernels,
  stage1/2/3 fits + CVs), `figures/*`, chain logs.

## Files Modified This Session
- New: `experiments/exp38_deposit_rethink/{README.md, shapes.py,
  stage0_diagnostics.py, stage1_shootout.py, stage2_multiepoch.py,
  stage2_family.py, stage3_inner.py}`.
- Updated: `doc/todo.md` (exp38 verdicts + the PARKED core entry),
  `doc/lessons.md` (+2), wensai journal (2 entries + snapshots).
- Previous session's work (exp36 multi-epoch + dressing) was merged at
  `839de70` before this session's exp38 work.

---
*Session: 1eae7122-a099-4237-ac37-9870f8f3eab0 — resume with
`claude --resume 1eae7122-a099-4237-ac37-9870f8f3eab0`*
