# Session Handover — 2026-06-22 (part 2)

## Goal
Build the "Ultimate SHMR": a portable, probabilistic model predicting a massive
central galaxy's stellar-mass profile from its halo. This session extended the
emulator with symbolic regression, a scatter model, a major data update, and the
discovery that **halo concentration adds real predictive power**. The model spec
is now frozen. **Next session: graduate the emulator into `hongshao/`.**

## Completed This Session (all merged to master + pushed; latest `61a960f`)
- **exp12 — PySR symbolic regression.** Installed PySR (Julia backend). The
  emulator mean is essentially linear; the only parsimonious nonlinear term is
  `late²` (outskirts) / `logtc·late` (core) — +2.1% CRPS, optional polish.
- **exp13 — outskirt-limit probe.** For M*[50–100 kpc], the MAH lifts R² 0.69→0.81
  but DiffMAH(4) is at the ceiling (MAH-PCA/raw-MAH add only +2.7%). Residual is
  intrinsic, not feature-limited.
- **exp14 — heteroscedastic scatter model.** `σ_j(X)=exp(γ·[1,X])` + fixed
  correlation R. Driven by `late`. Marginal CRPS ~flat but joint NLL +0.24 nats
  and conditional calibration gap 0.19→0.02. Gaussian adequate (PIT flat).
- **exp15 — the low-mass "bias" is regression to the mean**, not a defect
  (slope = −(1−R²) exactly; reliability slope +0.999). Use the model
  generatively (sample); the mean is unbiased in feature space.
- **DATA UPDATE — aperture table merged** (`hongshao/tng_data.load_aperture_extras`):
  exact z=0.4 halo mass `logmh_z0p4` (matches old proxy to +0.004 dex), secondary
  halo props (`c_200c`, 3D shape, `acc_rate`), 3 sky projections
  (`logmstar_aper_proj`). Projection scatter is small (~5% of variance). **Convention
  set: keep modeling the CoG-derived masses; `*_aper_proj` for cross-checks only.**
  `catgrp_id` (FoF GroupID) stored — but the official DiffMAH match is BLOCKED
  (id-system mismatch, see below).
- **exp16 — `c_200c` overturns the prior.** Concentration adds +5.0% CRPS on
  DiffMAH, **+2.7% even on MAH-PCA(4)** (genuinely MAH-independent; R²(c|MAH)=0.25).
- **exp17 — `c_200c` nonlinear limit.** Mostly linear (GBM≈linear); poly-2 best
  (+8.4% over DiffMAH-only) via `c_200c²`/`late·c_200c`; addendum `poly2_check.py`:
  gain is real (10/10 CV splits) but **diffuse** (~7 terms), BIC prefers a 7-term
  subset over full poly-2.
- **exp18 — only `c_200c` helps.** `acc_rate` MAH-redundant; 3D shape independent
  but irrelevant. ("All 4" ≈ `c_200c` alone.)
- **exp19 — folded `c_200c` into the heteroscedastic emulator.** Helps the MEAN
  (CRPS 0.0873→0.0832, +4.7%; NLL −3.349→−3.431, +0.08 nats) but NOT the scatter
  (`late` still drives σ). Conditional gap 0.018→0.010.

## In Progress (Not Finished)
- Nothing half-built. Next session is a fresh task: **graduate the emulator.**

## Problems / Blockers
- **Official DiffMAH/Diffstar cross-match is parked.** `catgrp_id` = TNG FoF
  GroupID; the catalog `halo_id` = global SubhaloID — different id systems, so
  equating integers reads satellites. Needs the TNG `GroupFirstSub` map, per-galaxy
  SubhaloID, or 3D positions. We use our own `dmah_*` fits meanwhile. (Not blocking.)

## Key Decisions
- **Working emulator (frozen):** linear mean on **`DiffMAH(4) + c_200c`** +
  **heteroscedastic full covariance** (variance on the same features), used
  **generatively** (sample, don't point-estimate — exp15).
- Default mean = linear; **optional** richer mean = the BIC-preferred 7-term
  degree-2 poly (exp17). `acc_rate`/3D shape dropped.
- CoG-derived masses are the modeling target (not `*_aper_proj`).

## Branch State
- Branch: `master`, clean, pushed (`61a960f`). No uncommitted changes.

## Files Modified This Session (key)
- Library: `hongshao/tng_data.py` (aperture-table merge; new columns).
- Experiments: `experiments/exp12..exp19_*/` (README + run.py; exp17 also `poly2_check.py`).
- Docs: `doc/{todo,lessons,tng300_data}.md`, `AGENTS.md`. Dataset rebuilt (gitignored).

## Next Session — graduate the emulator into `hongshao/`
- Build one module (e.g. `hongshao/emulator.py`) with `fit` / `predict` /
  `sample`, validated, with a `__main__` self-check:
  - **mean:** default = linear on `DiffMAH(4) + c_200c`; **option** = the 7-term
    degree-2 poly (`logmp²`, `logtc·late`, `early²`, `late·c200c`, `late²`,
    `logmp·early`, `logmp·late`).
  - **scatter:** heteroscedastic `σ_j(X)=exp(γ_j·[1,X_std])` (Gaussian MLE, ridge
    on slopes; exp14/exp19) + fixed residual correlation R → full `Σ(X)`.
  - **`sample()`** draws correlated, heteroscedastic profiles (the generative path).
  - Reuse `hongshao.metrics` for a CV self-check (CRPS/NLL/calibration); reproduce
    exp19 numbers (CRPS ~0.0832, NLL ~−3.43, cond gap ~0.010).
- Watch: keep it portable (DiffMAH + `c_200c` are N-body-available); don't switch
  the target to `*_aper_proj`; the model must be used generatively.
- Full context: this file, `doc/todo.md`, exp16–19 READMEs.

---
*Session: d3d2b4dd-1a7b-4223-8d35-e21ce5e3c232 — resume with `claude --resume d3d2b4dd-1a7b-4223-8d35-e21ce5e3c232`*
