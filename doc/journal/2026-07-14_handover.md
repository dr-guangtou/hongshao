# Session Handover — 2026-07-14

## Goal
The two-model program continues. This session: (1) closed out exp37 (the
multi-epoch statistical emulator, GRADUATED into `hongshao/multi_epoch.py`,
block-pinned product default + K=3 log-CoG product alongside); (2) opened
exp36 and completed its phase 1 — the two-channel deposit (A) + conditioned
theta (B) at z=0.4, merged and pushed. **Next: the exp36 multi-epoch round,
then (C) the statistical dressing.**

## Completed This Session (all merged to master and pushed; HEAD ed8c59e)
1. **exp37 graduated** (merge `45d1a04`): `hongshao/multi_epoch.py` carries
   the block-pinned profile product (DEFAULT: monotone AR(1)-coherent draws,
   uncertainty-weighted deficit allocation, draw planes ~floor through z=1)
   and the K=3 log-CoG product (best z=0.4 inner mean), behind
   `fit_multi_epoch(mode="block"|"cog")`; contracts pinned by the module
   self-check; poly2 cores default; broken-progenitor mask (idx 181, 2372;
   n=2395); manual §1.5 + API ref updated.
2. **exp36 phase 1** (merge `ed8c59e`): design review (6 measured problems),
   phase-0 residual anatomy (wall lower for exp35's variant, 0.67-0.75;
   unshared residual organized by fz2/t50/c200c, NOT burstiness; massive
   probe justifies the split), the build (nesting to exp35 exact at 1e-12),
   and the z=0.4 verdict: **2ch-prune adopted — 14 params, held-out pinned
   shape R>5 = 16.4%** (exp35: 19.6; unconstrained mark: 16.1; statistical
   wall: 15.6), width law OFF the rails (log_s0~2.05, g~3.1), massive
   dlog f148 +0.0149 -> +0.0023, in-sample 0.1544 < stress 0.1676 without
   horizon escape. Channel anatomy + parameter-halo relations figures;
   terminology rule (channel labels = placeholders; deposits BRING stars);
   low-mass outskirt overshoot measured (+0.13/+0.12 dex, 30-60/60-148 kpc,
   lowest tercile) as the next target; 77% of the deposited budget arrives
   from z>2 (the TNG-comparison mismatch is provenance/delay, not a z-cut).
3. qa.py polish (in the exp37 merge): usetex-safe text, distinct epoch
   colors, minimax gallery, draw overlays, fit-space density QA figure.

## In Progress (Not Finished) — the next session's task
The exp36 MULTI-EPOCH round, on a new branch (suggest `exp36-multi-epoch`),
same experiment folder:
1. Joint 5-epoch fit of 2ch-prune (`run.py` fit/cv take ks=[0..4] — the
   fit/cv plumbing already accepts epoch lists like exp35's JOBS; add
   multi variants to `cmd_fit` and run CV per epoch).
2. **The differential-deposition test must survive the new freedom** (fixed
   judged criterion; exp35's report machinery computes it).
3. **Bounds-stress log_s0_ex** (it rails at 3.0 in every fit): loosen to
   3.5 and check whether the fits re-open the >500-kpc deletion channel
   (visibility) or the rail is benign because f_ex prices it.
4. The low-mass outskirt overshoot fix: condition fa (the split amplitude)
   on c200c/fz2 or steepen fb; judged by the tercile dlog Sigma table
   (+0.13 dex must fall) WITHOUT losing the 16.4%.
5. Check the efficiency-peak (mu) stability between z04 and multi fits (P4).
Then (C): the statistical dressing on the kernel spine (exp37-style
compressed residual cores + AR(1)), with the flat-spine ablation guard.

## Problems / Blockers
- None blocking. Watch-outs: (1) `export
  HONGSHAO_DATA_DIR=/Users/shuang/Desktop/tng300_mah_mprof` for every run;
  (2) long fits: `nohup zsh -c '...' & disown` + log tail (harness kills
  background tasks); (3) reusing exp35's parallel fitter: spawn-safe worker
  pattern in exp36 run.py/phase0.py (path-loaded modules invisible to spawn
  children; fork corrupts the parent's open npz handles); (4) warm-start
  nested models from the fitted SUB-model's basin, nesting inequality as the
  convergence check (lesson, learned the hard way this session); (5) repo in
  Dropbox: trust `git diff HEAD` over phantom `MM` entries; (6) NEVER commit
  `doc/CLAUDE.md`; commit THIS handover on the next session's first branch;
  (7) usetex figure text: `<>|%` via qa helpers; (8) plain language for
  development methodology in communication (AGENTS.md rule).

## Key Decisions
- exp37: graduate BOTH block-pinned (default) and K=3 log-CoG products
  (conservative pairing, user decision); log-density variant stays
  experiment-local.
- exp36: no physicality mandate (user reframe) — best [MAH, halo] ->
  profile/CoG statistical model is the goal; physics labels are placeholders
  pending hydro-sim provenance checks (Pillepich+18 Fig 12 comparison is
  qualitative; fetch attempts truncated — user eyeballs it).
- 2ch-prune (conditioning only log_s0 and sig) adopted over the 23-param
  cond: identical held-out (ablation-confirmed), 14 params.
- E (step-conditioned split) stays parked: burstiness pre-signal weak (0.07).

## Branch State
- Branch: master, clean, pushed through `ed8c59e` (exp36-two-channel and
  exp37-multi-epoch-emulator both merged and deleted).
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit), this handover file.
- Gitignored outputs in place: exp36 `two_channel.npz` (thetas + CV mets +
  held-out cogs), `phase0.log`, figures; exp37 per-mode npz/figures.

## Files Modified This Session
- New: `hongshao/multi_epoch.py`; `experiments/exp36_two_channel/{README.md,
  phase0.py, run.py}`; `experiments/exp37_multi_epoch/` (closed out).
- Updated: `hongshao/{qa.py, emulator.py}`, `doc/emulator_manual.md`,
  `doc/lessons.md` (+4 lessons), `doc/todo.md`, `AGENTS.md` (communication
  + scope), wensai journal (2 entries + 2 snapshots).

---
*Session: ba7fa9b6-5fc9-40a3-bc59-44d31abe13b5 — resume with
`claude --resume ba7fa9b6-5fc9-40a3-bc59-44d31abe13b5`*
