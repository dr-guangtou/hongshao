# Session Handover — 2026-09-09 (exp78 closed and merged; exp80 proposed)

(The exp78 experiment-level record is `2026-09-09_exp78_handover.md`,
written before the merge; this is the session-level handover on top of it.)

## Goal

The user is steering the hongshao forward model (stellar-mass curves of
growth of massive centrals at five epochs from the halo's assembly history)
toward a mean model that passes the QA gates at high redshift. This session
closed exp78 (the size-aware objective) and the user's next ask is **exp80,
the deposit size law under the measured input**, plan
`doc/plans/2026-09-09-exp80-deposit-size-law.md` — awaiting the user's
approval of the plan (Stage 0 reads the size law from per-epoch
deconvolution BEFORE any fit).

## Completed This Session

- **exp78 done and MERGED to master (`1da18d2`), pushed.** Code and record
  in `experiments/exp78_size_aware_objective/` (`size_terms.py`,
  `stage0_terms.py`, `stage1_fit.py`, `stage1_eval.py`,
  `side_jump_cut.py`, `README.md`); outputs and figures gitignored under
  `outputs/` and `figures/qa/`.
  - Stage 0: the radius term (tercile-median log R20/R50/R80 offsets on the
    merged 0.673–148 kpc grid) passes the blind-spot probes and ranks the
    five known models as the gates do; exp77's annular term ranks the
    gate-rejected 14.63 basin best and is blind to a central point mass.
  - Stage 1 (12 + 0 parameters): the refit lands in the basin's family with
    the compact size railed at 31.6 kpc; offset gate 11 of 15 vs the
    baseline's 12; R50 at z ≥ 1.5 worse; envelope at z = 2 much better;
    widths up; centre worse. NOT adopted. Second basin at 16.31 bends the
    outer-vs-inner mass planes (a 0.14 dex compact/extended switch).
  - Stage 2 (13): g → −0.005; the size term does not rescue exp76's lever.
  - Side quest: a > 0.6 dex adjacent-epoch stellar-mass jump cut (45
    galaxies) moves only the z = 2 amplitude, by ~4 per cent; keep the rule
    until the snapshot check; quote z = 2 amplitude with ±4 per cent.
- Docs: `doc/lessons.md` (exp78 section + the outlier lesson),
  `doc/todo.md` (exp78 section with review), `doc/open_questions.md` (C24;
  C23 annotated), `CLAUDE.md` (exp79 taken by Codex, exp80 next free id),
  `doc/plans/2026-09-09-exp80-deposit-size-law.md`.
- Journal in the vault:
  `development/hongshao/hongshao_exp78-size-aware-objective_2026-09-09.md`;
  the one-sentence snapshot appended to today's daily note.
- Memory: `size-aware-objective-verdict.md`; `MEMORY.md` Current State.

## In Progress (Not Finished)

- **exp80, awaiting the user's decision on the plan.** Stage 0: run exp63
  Stage 1's NNLS deconvolution (`exp63/stage1_deconvolve.py`, currently
  z = 0.4 only — generalise to any epoch's curves) at every epoch; compare
  the data's compact and extended mode sizes and shares by halo-mass
  tercile with the model's own mass-weighted deposit sizes at the same
  epochs (`model2._deposits2` returns s_c, s_e per node with the deposit
  masses); choose the law change from the table; gate: > 0.02 dex of the
  z = 2 R50 offset recovered at frozen amplitude. Stage 1: the change under
  the STANDARD objective (no size term), four starts + continuation, the
  battery, the size gate (offset and width), the leak gate, the mass planes.
- Owed and not started: the v1 stochastic layer's re-baseline; exp76 option
  2 (g fixed by physics); C22; the jumper snapshot check; variant B2.

## Problems / Blockers

- **exp79 is TAKEN by the Codex agent** (`hongshao_exp79_deposition_reach`,
  branch `exp79-deposition-reach`: the deposit truncation boundary
  C × R200c at deposition, C = 3; clipping negligible; refits not
  converged; no model adopted). exp80 is the next free id as of 06:00 —
  check `git worktree list`, `git branch -a`, `ls experiments` again before
  creating. Do not change C in exp80; coordinate with exp79's result.
- The compact channel's size bound (1.5 dex = 31.6 kpc) binds under every
  size-aware start: a model-class statement, to be resolved by the size-law
  work, not by widening the box first.

## Key Decisions

- The radius term is the right size term; exp77's annular term is not.
- The weight-1 size-aware refit is not adopted (the gates decide); a size
  term, if kept, needs a gated weight sweep; a width term is the layer's.
- The high-z size excess is a model-class limit → the next step is a MODEL
  change (the size law), not another objective.
- The stellar-mass jumpers stay in the sample until the snapshot check.

## Branch State

- Branch: `master` at `1da18d2` (+ this handover), clean, pushed.
- `exp78-size-aware-objective` merged and deleted. No branch of ours open.
- Worktrees: exp50/51/55 (prunable), internal-review, and Codex's
  `hongshao_exp79_deposition_reach`.

## Files Modified This Session

- `experiments/exp78_size_aware_objective/*` (new)
- `doc/lessons.md`, `doc/todo.md`, `doc/open_questions.md`, `CLAUDE.md`
- `doc/plans/2026-09-09-exp80-deposit-size-law.md` (new)
- `doc/journal/2026-09-09_exp78_handover.md`, this file
- memory: `size-aware-objective-verdict.md`, `MEMORY.md`

## Ops notes (carry forward)

- Every fit and judge under nohup + `caffeinate -i -w <pid>`; five parallel
  processes took 26–27 min per 3000 evaluations.
- `stage1_fit`, `rebaseline` and `stage0_terms` are shadowed by other
  experiments' module names; import by file path.
- Poll on the final npz name, not on `grep "^wrote"` (figure lines match).
- Name merged files by the stage that made them, once, in a shared dict.

---
*Session: 39e026df-f8ae-44da-8e95-55da677e7d72 — resume with `claude --resume 39e026df-f8ae-44da-8e95-55da677e7d72`*
