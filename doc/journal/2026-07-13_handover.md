# Session Handover — 2026-07-13 (exp35: total-normalized transport refit + bounds-stress verdict)

## Goal
The Ultimate-SHMR forward emulator. This session executed **Path B — the
TOTAL-NORMALIZED transport refit (exp35)** end-to-end and closed it with a
verdict. **Next: Path A (the multi-epoch statistical emulator from the
exp33-vi blueprint) as the product baseline; exp36 two-channel deposit as the
motivated transport-kernel modification (user decides which first).**

## Completed This Session (all merged to master and pushed; HEAD 46c1f4e)
1. **exp35 machinery** (`experiments/exp35_total_norm/run.py`, commit adfd88b):
   exp33's 5-param physical theta (alpha==1, lognormal f(z)) with the dyntrans
   basis extended to 500 kpc; model CoG pinned per epoch to M(<500) — the
   aperture fraction M(<148)/M(<500) becomes a fitted datum, replacing the
   148-kpc pinning that enabled the aperture-horizon deletion channel. Loose
   box log_s0<=3.0, g<=4.0. Subcommands: totals/fit/cv/report/stress/demo.
   Demo: extended basis == tf.basis exactly; 500-pinned model re-pinned at 148
   == exp33 physical model to 1e-10.
2. **M(<500) data** (`totals` -> outputs/m500.npz): 2397x5 power-tail refits,
   0 failures; M_tot_inf == exp34 to 0.0000 dex; form systematic at 500 kpc
   0.011 dex. Beyond-148-within-500: 7.2% (z=0.4) -> 1.0% (z=2); data f148
   falls 0.94 -> 0.83 with mass.
3. **Fits + CV verdict** (commit 99a6917, README): all four fits (z04/multi x
   global/slope) in ONE basin; every z>=1 bin >=95% visible in 148 kpc.
   Held-out 148-pinned shape R>5 kpc at z=0.4: z04-slope 19.6% (marks: 16.1
   unconstrained / 20.5 exp33 box), multi-slope 20.5% (mark 19.1), epoch-avg
   19.7%. Physicality tax +3-4.4 -> +1.4-2.3 points; f148 reproduced to
   0.016-0.004 dex. **Differential-deposition test PASSES for multi-slope**:
   massive tercile z0.7->0.4 beyond 50/100 kpc measured 0.37/0.11 vs model
   0.40/0.12, mass trend reproduced; mass-flat global FAILS (0.29/0.08) —
   theta(logMh) carries the physics.
4. **Bounds-stress test** (commit f10b560, `stress` subcommand): box ->
   (3.5, 6.0): loss improves 4.3% (0.1751->0.1676, two starts agree) BUT
   log_s0 rails again, z[1,2) visibility 0.95->0.62, z<1 deposits 91%
   invisible even within 500, and the measured observables (massive f148
   0.920->0.913 vs data 0.883; marks 0.41/0.13) do NOT improve. The rail is
   horizon escape, not physics. Lesson added to doc/lessons.md.
5. Docs: exp35 README (full verdict tables), todo.md exp35 entry closed,
   journal entry `wensai/development/hongshao/hongshao_master_2026-07-13.md`.

## In Progress (Not Finished)
- Nothing in flight. exp35 is closed. Two queued directions, user to pick:
  1. **Path A (product)**: multi-epoch statistical emulator — continuous-z
     coefficient interpolation (closure +-4%) + AR(1)-in-epoch latent
     (rho=0.67) + generative sampling (passes 2-D planes). Spec: exp33 README
     step vi + doc/todo.md "next A".
  2. **exp36 (physics): two-channel deposit** — compact in-situ + wide
     ex-situ component per deposit, mass-dependent split (+2-3 params on the
     exp35 machinery). Targets: the 4.3% stress headroom, railed g, massive-
     end f148 under-spread (model 0.875 vs data 0.83). Judges: f148(logM*) +
     differential-deposition curve (out-of-model). Note exp30's v2 gate
     (burstiness null) rejected an ex-situ channel — re-test against the NEW
     observables, not burstiness.

## Problems / Blockers
- None open. USER'S CLOSING CONCLUSION (frame the next session with this):
  the transport kernel, though physically driven, structurally demands an
  unphysical super-diffuse component — the single global width law can only
  produce the real massive-end outward transport by dragging recent deposits
  past any normalization horizon. Per-epoch normalization at ANY finite
  radius leaves an invisible channel beyond it; only a structural DOF
  (two-channel) can decouple it.
- Watch-outs (standing): (1) long fits: `nohup zsh -c '...' & disown` +
  log Monitor (harness kills background tasks); (2) macOS Pool needs
  initializer args; (3) railed tail slopes make M_inf soft — always use
  finite-radius M(<500); (4) hongshao/qa.py after every fit, quote all-R AND
  R>5 kpc; (5) figures: surface path + content every turn; (6) NEVER commit
  doc/CLAUDE.md; (7) `import run` collides across experiment dirs — exp35
  loads exp34/exp33 modules via importlib `_load_by_path` (copy that pattern).

## Key Decisions
- Keep the physical box at (3.0, 4.0): exp35 headline numbers are the honest
  ones; looser bounds re-open deletion at the 500-kpc horizon (stress test).
- exp35 verdict on the Path A vs B decision: the fraction datum pays the
  physicality tax (not the ~3-point consistency tax, as expected). Path A =
  accuracy product; transport kernel = physics companion with a passed
  out-of-model test (differential deposition).
- Judged marks stay the fixed reference set: 15.6/16.1/19.1% shape R>5 kpc,
  identical folds (i % 10) via um.load_gals ordering.

## Branch State
- Branch: master, clean (only doc/CLAUDE.md untracked — NEVER commit).
- Pushed through 46c1f4e (merge exp35-total-norm; branch deleted).
- Key gitignored outputs: exp35 m500.npz, thetas.npz, total_norm.npz (CV
  metrics + held-out cogs), stress.npz, full_run.log, stress_run.log,
  figures (exp35_total_norm.png + QA sets for z04-slope/multi-slope).

## Files Modified This Session
- New: `experiments/exp35_total_norm/{run.py, README.md}` (+ gitignored
  outputs/figures above).
- Updated: doc/todo.md (exp35 closed), doc/lessons.md (+1: bounds-stress a
  railed bound before reading it), doc/journal/2026-07-12_handover.md
  (committed), this handover.

---
*Session: c44c9543-43c9-420b-a5b7-241d7b981145 — resume with `claude --resume c44c9543-43c9-420b-a5b7-241d7b981145`*
