# Session Handover — 2026-07-18 (session 3: exp41, the stochastic layer)

## Goal
exp41 (the stochastic layer, exp32 step 4) is COMPLETE, ADOPTED, and
merged. **The adopted product: the official z<=1.5 kernel + the 1-D
empirical stochastic layer as its generative companion.** Next
session: no task in flight; candidates below.

## Completed This Session (all merged to master `8b7d78f` and pushed)
1. **Stage 0 — anatomy + alignment**
   (`experiments/exp41_stochastic_layer/stage0_anatomy.py`, n=2397):
   the kernel's per-galaxy individuality is ONE latent axis (any
   single freed component buys 18.5-22.3%; all six deltas
   rank-correlate at |rho| >= 0.92); it IS the exp39 core-diversity
   axis (|rho| = 0.78-0.87 vs per-galaxy f_core) and is
   feature-orthogonal (|rho| <= 0.20) — pure per-object information.
   Dimensionality (joint 2-D refit): median second-dof gain 1.9
   points, but 19% of galaxies gain >10 (fat second-axis tail).
2. **Stage 1 — the deviation distributions** (`stage1_deviation.py`):
   d_sig heavy-tailed (robust sigma 0.076-0.081, t-dof 4-9); d_q
   near-Gaussian (0.278); correlation -0.31; box pile-up ~2%.
3. **Stage 2 — judged draws** (`stage2_draws.py`, held-out
   symmetrized split): the 1-D empirical layer takes the kpc planes
   from 1.6-2.1x split-half floor to **1.0-1.4x at z<=0.7 (target ~1
   MET at z=0.4)**; physics in the adopted band (differential
   0.40/0.12, overshoot T1 +0.027/+0.031); mean model untouched.
   Gaussian uniformly worse; 2-D adds nothing and strains the
   overshoot.
4. **The Re-preserving refinement: REJECTED, doubly informative** —
   pinning drawn sizes destroyed the kpc gains (the layer's diversity
   IS size diversity), and it exposed the paired-metric artifact:
   qa's Re apertures (truth R_half on the model) punish independent
   draws. Rescored SELF-CONSISTENTLY the 1-D layer is 1.0-1.4x floor
   at EVERY epoch on the Re plane — resolves the exp33 "fails only in
   Re coordinates" puzzle. Lesson added to doc/lessons.md.
5. **ADOPTION (user)** recorded in the exp41 README + doc/todo.md;
   reference implementation `stage2_draws.draw_deltas`/`drawn_cogs`,
   deviation pool `outputs/stage1_dist.npz`.
6. **Standard QA figure set** (7 figures, mean + draw overlay)
   generated via `stage2_draws.py report` and delivered
   (`figures/qa_*_exp41_kernel_layer.*`). All five-epoch outputs use
   the z<=1.5 kernel; the z=2.0 column is PURE EXTRAPOLATION (the
   documented dissipative-era diagnostic; user briefed).

## In Progress (Not Finished)
- **NEXT SESSION (user direction, 2026-07-18): journal-style
  TECHNICAL DOCUMENTATION of the two emulators**, in a new
  `doc/tech_note/` folder, staged:
  - **Stage 1 — the PCA-based (statistical) emulator**: the graduated
    stack `hongshao/emulator.py` (heteroscedastic conditional
    Gaussian on [DiffMAH(4) + c200c]) -> `profile_emulator.py`
    (profile representations incl. the PCA modes of the log growth
    curve) -> `multi_epoch.py` (the exp37 multi-epoch generative
    product: continuous-z coefficient interpolation, AR(1)-in-epoch
    latent, block-pinned reconstruction). Source records: exp19/22/33
    and the exp37 README; also `doc/emulator_manual.md`.
  - **Stage 2 — the transport-kernel emulator**: the deposition
    lineage (exp29 -> 30 -> 35 -> 38) culminating in the adopted
    1ch-mof kernel (lognormal efficiency window, power-law-tail
    deposits, the dyntrans migration clock, M(<500) normalization,
    halo conditioning) at the OFFICIAL z<=1.5 scope (exp40) + the
    exp41 1-D empirical stochastic layer. KERNEL-IDENTITY NOTE
    (user asked 2026-07-18): the ADOPTED kernel is the SINGLE-channel
    1ch-mof (user decision 2026-07-16); the two-channel 2ch-exp (16p)
    is the documented accuracy ALTERNATIVE, never adopted and never
    re-fit at the z<=1.5 scope; exp36's 2ch-fa was the pre-exp38
    incumbent, retired. Pedagogical beat for the note: the
    two-channel split was the natural first fix for the missing
    outskirt light, superseded because a single heavy-tailed deposit
    makes the split unnecessary — worth one subsection + figure.
  - Audience: understands the general motivation, has NOT seen the
    experiments — every model ingredient explained carefully, with
    LaTeX equations.
  - **Visualization is the key deliverable** — pedagogical
    ILLUSTRATIONS, not QA figures: e.g. what each PCA component does
    to a profile (vary one coefficient, show the family of curves),
    the deposition-then-migration behavior (one galaxy's profile
    assembling from deposits across cosmic time), the kernel shape
    anatomy (why the power-law tail vs a Gaussian), the efficiency
    window, the stochastic layer's diversity axis. Small GIF
    animations welcome (matplotlib FuncAnimation -> pillow writer);
    e.g. deposits arriving + migrating as cosmic time runs.
  - Format DECIDED (user 2026-07-18): Markdown with LaTeX math
    (Obsidian/GitHub-rendered). Staging DECIDED: stage 1 = the
    statistical emulator, stage 2 = 1ch-mof (the adopted kernel),
    stage 3 = 2ch-exp (the two-channel alternative — presented as
    its own model, including why the adopted kernel supersedes the
    split).
  - Figure hygiene: this machine's matplotlibrc has usetex=True
    (route text via `hongshao.qa._tex`/`_pct` or plain math-safe
    strings); the user reviews figures closely.

## Problems / Blockers
- None. Watch-outs: (1) `export
  HONGSHAO_DATA_DIR=/Users/shuang/Desktop/tng300_mah_mprof`; (2) long
  fits via nohup + caffeinate -im + disown with a log tail (macOS has
  no /proc — poll with pgrep, not /proc); (3) SCORING CONVENTION now
  split by model class: paired-aperture metrics for mean predictions,
  population metrics (own-size apertures, `cmd_replane` pattern) for
  draws; (4) the g=4.0 rail is benign in the z<=1.5 scope, bad-basin
  in the 5-epoch scope; (5) `rm` aliased to Trash-move; (6) never
  commit doc/CLAUDE.md.

## Key Decisions
- **The 1-D empirical layer ADOPTED** (user): smallest sufficient
  dimensionality, empirical (heavy tails beat Gaussian), mean-zero,
  box-clipped; scope documented (kpc planes z<=0.7-1.0; z>=1.5
  residual belongs to the mean model).
- The Re-preserving refinement rejected on measurement; the 2-D
  variant rejected (no plane gain, overshoot strain).
- The z=2.0 column stays in the standard QA as the honest
  extrapolation diagnostic of the dissipative era.

## Branch State
- Branch: master, clean, pushed through `8b7d78f`
  (exp41-stochastic-layer merged and deleted).
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit) and this
  handover (commit as the next branch's first commit).
- Gitignored outputs: exp41 `outputs/*.npz` (anatomy, anatomy_pair,
  stage1_dev2, stage1_dist, stage2_scores, stage2_refine,
  stage2_replane) + logs; `figures/qa_*_exp41_kernel_layer.*`.

## Files Modified This Session
- New: `experiments/exp41_stochastic_layer/{README.md,
  stage0_anatomy.py, stage1_deviation.py, stage2_draws.py}`,
  `doc/journal/2026-07-18_handover_3.md` (this file).
- Updated: `doc/todo.md` (exp41 adoption entry), `doc/lessons.md`
  (+1 paired-metric lesson), wensai journal
  `hongshao_master_2026-07-18_3.md` + snapshot.

---
*Session: f1d741b1-9def-45a0-843b-46cee79308d1 — resume with
`claude --resume f1d741b1-9def-45a0-843b-46cee79308d1`*
