# Session Handover — 2026-09-22

## Goal
The hongshao mean-model programme (roadmap
`doc/plans/2026-09-12-roadmap-evidence-rethink.md` §5). This session
(2026-09-13/14 and 2026-09-21/22) ran and closed **exp83 — roadmap S3, the
early-mass conditioning of the centre on the adopted exp82 mean**. The next
session starts **exp84 — roadmap S4, the stochastic layer re-baselined on
the adopted mean with an outer size component** (roadmap §3 S4; a Codex
candidate per §5).

## Completed This Session
- exp83 Stage 0 (the frozen probe, no fit): the adopted mean's z = 0.4
  residual against the halo mass fraction assembled by 2 Gyr is linear and
  is two components — a total-mass part (+0.114 dex/dex at 103 kpc) and a
  concentration part (+0.059 at 5 kpc at fixed M(<103)); the early-formed
  haloes are the central decliners. One per-deposit variable with three
  placements, all nesting at zero, built into the engine
  (`experiments/exp80_deposit_size_law/size_law.py`: knobs `a_early`,
  `s_early`, `c_early`; `set_early_ref` — the population-median reference
  MUST be set before use, `stage1_fit.build` does it;
  `model2.compact_share(logit_extra=)`). Thirteen probes: a_early ≈ −0.28
  zeroes the 103 kpc correlation; nothing reaches 5 kpc (the residual is
  in the pre-2-Gyr deposits, where the variable is zero by construction).
  The user's gate failed at every point; Stage 1 withheld.
- exp83 Stage 1 (the user's decision 2026-09-21 to fit the mass half
  anyway): three starts from the adopted mean, one basin, a_early → −0.08,
  loss 12.70 → 12.65, the z = 0.4 correlation +0.33 → +0.26 (103 kpc) /
  +0.37 → +0.33 (5 kpc), every gate within resolution of the adopted mean.
  15 parameters (14 fitted). **NOT adopted (the user, 2026-09-22); the
  exp82 mean stands as `rebaseline.adopted_mean()`.**
- Record: `experiments/exp83_early_mass_centre/README.md` (the full
  verdict), `doc/plans/2026-09-13-exp83-early-mass-centre.md`, roadmap §7,
  `doc/todo.md`, four lessons in `doc/lessons.md`, `doc/open_questions.md`.
  Vault journal `development/hongshao/hongshao_master_2026-09-22.md`; the
  daily note's Dev Snapshot.
- Merged to master `fdd99d9` (`--no-ff`), pushed; branch deleted.
  Gitignored outputs in `experiments/exp83_early_mass_centre/outputs/` and
  `figures/qa/*exp83_*`.

## In Progress (Not Finished)
- Nothing running. exp84 not started. Its plan (roadmap S4): re-run
  exp60's layer stages on a predictor that takes the measured history and
  the adopted mean (`rebaseline.adopted_mean()["predict"]`); add a second
  size draw for the extended component; gate at R20 / R50 / R80 with offset
  and width separate (`exp73/size_gate_layer.py` scores draws); make tier
  2e read the draws. The v1 layer (exp60) runs on exp57's X3 problem and
  the official-curve engine (`engine.build_curves` asserts it) — that is
  the half-day rebuild `rebaseline.py`'s docstring records as owed.

## Problems / Blockers
- The centre's concentration residual (the decline) needs a mechanism that
  moves mass outward in early-formed galaxies between z = 2 and z = 0.4
  (an age-driven expansion of the compact channel is the candidate) — a
  model change outside S3, logged in `doc/open_questions.md`; its own
  experiment id after exp84.
- Costs carried with the adopted mean (C26) unchanged: the mh-complete
  50–100 kpc shell at z ≥ 1.5 −12 / −18 per cent; the future-dependence
  gate +0.03 at z = 0.7 and z = 2.

## Key Decisions
- The gate (both radii halved) failed, so no fit was run until the user
  chose to fit the mass half; the fit confirmed the frozen probe's
  forecast (the loss column was flat to −0.1) — non-adoption was the user's
  call on that evidence.
- The early-mass variable is taken RELATIVE to the population median at
  each time (the raw variable is a second time law).

## Branch State
- Branch: `master` at `fdd99d9`, clean, pushed (== origin/master).
- exp84 is the next free id; exp79 is Codex's (worktree
  `hongshao_exp79_deposition_reach`). Check `git worktree list`,
  `git branch -a`, `ls experiments` before creating.

## Files Modified This Session
- `experiments/exp83_early_mass_centre/*` (new: `stage0_probe.py`,
  `queue.sh`, `judge.sh`, `README.md`),
  `experiments/exp80_deposit_size_law/{size_law,stage1_fit,stage1_eval,stage0_candidates}.py`,
  `experiments/exp63_analytic_growth/model2.py`, `CLAUDE.md` (the id list),
  `doc/plans/2026-09-13-exp83-early-mass-centre.md` (new),
  `doc/plans/2026-09-12-roadmap-evidence-rethink.md`, `doc/todo.md`,
  `doc/lessons.md`, `doc/open_questions.md`, this file.

---
*Session: 4e010ef3-f800-4539-aca5-9e3513edfb52 — resume with `claude --resume 4e010ef3-f800-4539-aca5-9e3513edfb52`*
