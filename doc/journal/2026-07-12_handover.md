# Session Handover — 2026-07-12 (exp31–34: QA harness, full population, single-epoch consolidation)

## Goal
The Ultimate-SHMR forward emulator. This multi-day session built the
standardized QA, scaled to the full population (n=2397), consolidated the
single-epoch (z=0.4) prediction to a written verdict, discovered and resolved
the aperture-horizon degeneracy, and measured the beyond-aperture mass budget.
**Next session: Path B — the TOTAL-NORMALIZED transport refit** (Path A, the
multi-epoch statistical emulator from the exp33-vi blueprint, queued after).

## Completed This Session (all merged to master and pushed; HEAD 646a160)
1. **exp31** — `hongshao/qa.py` standardized tiered QA (apertures/annuli/
   envelopes kpc+Re, observational planes, dual profile metric, visual QA) +
   LOGO scoreboard. Plane fidelity = 2-D energy distance / split-half floor,
   full + centered (location vs shape decomposition).
2. **exp32** — full population (n=2397, logM* 10.66–12.36): per-galaxy θ atlas
   (~10% floor holds everywhere); universal θ 30.4% held-out with ZERO CV gap
   (capacity); θ(logMh) adopted (29.4%); anatomy: individuality = degenerate
   width-growth × efficiency-shape subspace, NOT size (log_s0 closes 0.2%);
   epoch-matched features overturned the "MAH decays with z" reading.
3. **exp33** — single-epoch consolidation, verdict in its README:
   - generative `sample()` PASSES the 2-D planes (0.8–1.0× floor, native
     targets); PCA-3 fails only in Re coordinates (information, not basis);
   - features at limit (+2.1% ceiling; burst +1.3% real, not adopted);
   - statistical vs transport residuals ρ=0.82–0.89 → one information wall;
     projection floor measured 5× below current errors;
   - 2×2 completed: transport form costs 0.5 points, consistency 3.0
     (single-epoch transport θ(logMh): 16.1% vs statistical 15.6%);
   - representation repairs (size-aware/core-split/density): clean negative;
   - epoch connection: cross-epoch residuals AR(1) ρ=0.67; coefficient-
     interpolation closure ±4% → multi-epoch statistical blueprint measured.
4. **DISCOVERY: aperture-horizon degeneracy** — population transport fits
   delete epochs geometrically (z=1–2 deposits, 14% of the budget, at 550+
   kpc widths = 4% visible), enabled by per-epoch aperture pinning. Physical
   5-param refit (α≡1, lognormal f(z), bounded widths; `physical_theta.py`):
   physicality restored at +3–4 points, g rails at its bound.
5. **exp34** — asymptotic totals via CoG extrapolation (`asymptotic_total.npz`):
   power tail truncation-validated (0.0026 dex); **f_out real: 12% median,
   26% massive quartile at z=0.4**, 1% by z=2; differential deposition:
   37%/11% of z=0.7→0.4 growth beyond 50/100 kpc (massive tercile).

## NEXT SESSION — the total-normalized transport refit (todo "next B")
Build on `experiments/exp33_single_epoch/physical_theta.py` (5-param physical
θ machinery) + `experiments/exp34_asymptotic_total/outputs/asymptotic_total.npz`:
1. Normalize the transport model to **M_tot(<500 kpc)** computed from the
   exp34 power-tail fits (finite radius — NOT the formally infinite total;
   recompute M(<500) from the tail params or refit; npz stores M_tot∞, f_out,
   tail_slope; A-coefficient not stored → cheap refit or extend run.py).
2. The per-epoch aperture fraction M(<148)/M(<500) becomes a FITTED DATUM
   (append to the loss), replacing per-epoch pinning — the deletion channel
   becomes falsifiable; widths may then be unbounded or loosely bounded.
3. Judge: held-out per-epoch shape (R>5 kpc) vs the 16.1/19.1% marks; θ
   physicality (visible fractions); epoch stability of θ; and NEW — the
   **differential-deposition test**: does the fitted width law reproduce the
   measured "fraction of inter-snapshot growth beyond 50/100 kpc" curve?
4. Decision: does the transport family close its ~3-point consistency tax
   when the beyond-aperture budget is data? → feeds the Path A vs B choice.

## Problems / Blockers
- None open. Watch-outs: (1) harness background tasks were killed repeatedly —
  long jobs must be launched `nohup zsh -c '...' & disown` (orphaned) with a
  log tail Monitor (lesson in doc/lessons.md); (2) multiprocessing Pools need
  initializer args on macOS spawn (two crashes from this); (3) f_out for
  galaxies with tail slope a≈0 (railed) is soft — use finite-radius totals;
  (4) exp32 population-θ VALUES are uninterpretable (degeneracy) — only
  performance numbers carry.

## Key Decisions
- Multi-epoch model did NOT graduate (user); single-epoch stack stays as-is
  with documented error budget (exp33 verdict).
- DiffMAH-input configuration = product standard (validated at population
  level); frozen features [DiffMAH(4)+c200c] stand.
- Totals from OUR CoG extrapolation, not SubhaloMassType (definition
  mismatch; user). "Beyond-aperture fraction", not "ICL" (terminology).
- QA standards now include: energy planes (full+centered), input-binned bins
  figure with raw AND amplitude-pinned residuals, R>5 kpc quoted beside all-R.

## Branch State
- Branch: master, clean (only doc/CLAUDE.md untracked — NEVER commit).
- Pushed through 646a160 (merges: exp31, exp32, exp33+34).
- Key gitignored outputs: exp32 population.npz + atlases + um_* CVs; exp33
  single_epoch.npz, transport_z04.npz, physical_theta.npz; exp34
  asymptotic_total.npz.

## Files Modified This Session
- New: `hongshao/qa.py`; experiments exp31_forward_scoreboard/,
  exp32_full_population/ (dataset, theta_atlas, universal_mass, scoreboard),
  exp33_single_epoch/ (run, features, head2head, repr_fix, epochs,
  transport_z04, physical_theta), exp34_asymptotic_total/run.py + READMEs.
- Updated: doc/todo.md, doc/lessons.md (8 new lessons), exp30/31 READMEs,
  `experiments/exp30_transport_kernel/{pop_forward,profile_qa}.py` (exp30
  phase 4, merged earlier).

---
*Session: 6135c623-9942-459f-8001-73abc2a286a5 — resume with `claude --resume 6135c623-9942-459f-8001-73abc2a286a5`*
