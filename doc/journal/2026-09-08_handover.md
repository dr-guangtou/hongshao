# Session Handover — 2026-09-08

(The overnight run's own record is `2026-09-08_overnight_handover.md` with its
morning addendum; this file is the session-level handover on top of it.)

## Goal

The user is steering the hongshao forward model (stellar-mass curves of
growth of massive central galaxies at five epochs, predicted from the halo's
assembly history) towards an honest input and an objective that scores what
the QA gates score. The immediate ask at the end of this session: **review
the programme's history of objective choices and propose, in plain
language and in detail, a SIZE TERM for the objective** — the objective
currently scores no size at all, and has now rejected a gate-preferred basin
(C23) and a gate-preferred lever (exp76) in one night. The user's reminder to
carry into that design: in kpc-defined apertures at high redshift there is
little mass in the outskirts, so a small absolute offset becomes a huge
relative error (exp73 / C21: two galaxies carried 78 per cent of the z = 2
loss on fixed kpc).

## Completed This Session

- **exp73 merged** (size-relative objective is a dial; the size gate joined
  the QA battery; the v1 stochastic layer scored on it — see the 2026-09-01/02
  handovers).
- **exp74 done and merged** (`360d2d6`): C19 answered — the official DiffMAH
  curve let the final halo mass rewrite the early history; two honest inputs
  (a DiffMAH fitted before each epoch; the measured merger-tree history)
  give the same model at 11–13 per cent loss cost and bring the z = 2
  massive progenitors from −9.7 to −1.1 per cent. THE HALO INPUT rule in
  `CLAUDE.md`: the full MAH incl. the final halo mass is legitimate; the
  galaxy's own stellar mass never is; a standing future-dependence QA gate;
  `exp74/measured.py::build_input(recs, kind)` serves `official` /
  `pre-epoch` / `measured`.
- **Adoption + re-baseline merged** (`1fcdc08`, this morning, after a
  conflict with the Codex agent's concurrent merges — resolved by keeping
  both sides of `doc/lessons.md`): the measured history is the application
  default; references rebuilt at the nested incumbent on measured curves,
  bins by the measured mass; the incumbent re-baselined (16.14, single
  basin); **exp74's measured optimum ADOPTED as the baseline mean**
  (`exp74/rebaseline.py::adopted_baseline()`, rule in `CLAUDE.md`); the
  lower-loss basin (14.63, a 25 kpc "compact" channel with 44 per cent of
  the deposits) recorded as gate-rejected (C23, the fifth loss-vs-gates
  case). The honest input's per-galaxy cost at high z explained (the
  official curve denoised each galaxy's early history; the honest inputs'
  larger early halo radius pushes the extended deposits outward: 50–100 kpc
  shell at z = 2 +30 per cent per galaxy vs −2).
- **exp76 run, not adopted, not closed** (branch `exp76-growth-rate-split`,
  worktree `hongshao_exp76_growth_rate_split`, pushed): the deposit's
  compact/extended choice depending on the halo's growth rate is rejected by
  the loss (g stays at 0) and wanted by the gates (z = 2 outer size spread
  0.22 → 0.48 of the truth's with g = −0.27, both centres improve). C22: the
  data's compact share follows DiffMAH's parameters, not measured formation
  time.
- Journals in the vault: `development/hongshao/hongshao_exp73-…_2026-09-02.md`,
  `…exp74-c19-history-leak_2026-09-07.md`, `…adopt-measured-input_2026-09-08.md`.

## In Progress (Not Finished)

- **The size-term proposal** (the user's ask; nothing written yet). Inputs
  to it: exp48 (a density/differenced objective is blind inside the first
  radius — include the aperture as bin 0), exp63 (`--objective binned`: the
  tercile-median gate the per-galaxy loss cannot see), exp72 (the measured
  error model is a centre/low-z weighting; C17/C20), exp73 (fixed kpc is a
  weighting; the relative residual blows up in empty high-z outskirts; the
  mass-weighted residual on the truth's R50 coordinate; the R20/R50/R80
  size gate with offset and width sub-gates; the cost of sharing +20 per
  cent under R50 vs +28 under kpc; C21), exp77 by the Codex agent (an
  ANNULAR-mass loss fixes the z = 2 envelope from +88 to +5 per cent but
  worsens R50 at z = 1.5 by 0.02 dex and the future-growth response; closed
  as informative; its stated next step is a joint CoG/annular/size loss —
  read its README "Result in plain language" first).
- Owed after the proposal: re-tune the extended deposit's r200 scaling under
  the measured input; a light per-galaxy smoothing of the measured history
  (variant B2); the v1 stochastic layer's re-baseline (exp60 stages 1–3 on a
  measured-history predictor, ~half a day); exp76's fate (a size term then
  refit with g free, or g fixed by physics, or close).

## Problems / Blockers

- **A parallel OpenAI Codex agent works in this repo** in its own worktrees
  (`hongshao_exp75_…`, `hongshao_exp77_…`, `hongshao_integrate_*`) and merges
  to master on its own schedule. It took exp75 and **exp77**; the next free
  id is exp78 — check `git worktree list` and `ls experiments` before
  numbering, and expect `doc/lessons.md` / `doc/todo.md` conflicts on merge
  (both sides append; keep both).
- The objective scores no size; every size-improving change so far has been
  rejected by it (C20, C23, exp76).

## Key Decisions

- The measured history is the application default; DiffMAH inputs stay
  supported (`build_input`).
- exp74's measured optimum is the baseline mean; the gates decide over the
  loss (the user, 2026-09-08).
- The growth-rate split is not adopted (the loss rejects it).
- The size-term design is the next step, to be discussed with the user
  before any fit.

## Branch State

- This working directory: DETACHED at master `1fcdc08` (master itself is
  checked out in the `hongshao_master` worktree — merge from there). Clean.
- `exp76-growth-rate-split`: open, pushed, not merged (its worktree's
  `experiments/*/outputs` are symlinks to this checkout's outputs).
- `adopt-measured-input`: merged and deleted.

## Files Modified This Session (since the exp74 merge)

- `experiments/exp74_c19_history_leak/measured.py` (`DEFAULT_INPUT_KIND`),
  `rebaseline.py`, `rebaseline_eval.py`, `README.md`
- `experiments/exp76_growth_rate_split/{stage0_leverage,stage1_fit,stage1_eval}.py`,
  `README.md` (on the exp76 branch)
- `CLAUDE.md` (THE HALO INPUT rule + THE BASELINE MEAN), `docs/SPEC.md`
- `doc/open_questions.md` (C22, C23), `doc/lessons.md`, `doc/todo.md`,
  `doc/journal/2026-09-08_overnight_handover.md`
- memory: `adopted-references-two-basins.md`, `growth-rate-split-verdict.md`,
  `pre-epoch-history-removes-the-leak.md`, `MEMORY.md`

---
*Session: aeadebe5-df44-4d2d-b692-7248e605894b — resume with `claude --resume aeadebe5-df44-4d2d-b692-7248e605894b`*
