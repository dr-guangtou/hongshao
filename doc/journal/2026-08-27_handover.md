# Session Handover — 2026-08-27

## Goal

The 2026-08-26/27 session executed the full reassessment cycle the previous
handover asked for, then the user-directed follow-ons. **The cycle is CLOSED
and MERGED TO MASTER (pushed, `85740a7`).** The user's closing verdict, now
recorded in exp60's README: *"we might be seeing the limitation of the
deposition-only model's capability to emulate the multi-epoch CoGs of
massive galaxies."*

**NEXT SESSION, the user's instruction: add QA figures and visualization to
the exp61 results** — the single-epoch baseline table and the
parameter-drift-vs-redshift table (`experiments/exp61_epoch_baselines/`,
data in `outputs/stage0_fits.npz` and `outputs/stage1_mx2_fit.npz`) exist as
numbers only. Follow the project QA figure standards (usetex-safe text,
Okabe-Ito epoch colors — see `hongshao/plotting.py` and the exp59
`stage0_figure.py` for a recent in-style example).

## Completed This Session

Read these, in order, for the full record — each is self-contained:

1. `experiments/exp58_reassessment/README.md` — P-A (transport comparison
   settled like for like: +3%, not +42%), P-B (z=2 characterised), P-C (the
   amplitude ceiling ladder), P-D (the decliner compromise: error sum 0.194
   vs floor 0.062).
2. `experiments/exp59_conditioned_efficiency/README.md` + `PROBLEMS.md` —
   all five conditioning variables disqualified pre-fit by the leverage
   sweep (figure `figures/exp59_leverage.png`); the pinned-size X3 refit
   (drift eliminated, loss and gates agree for the first time; NOT adopted
   — user); the B-gate battery `b_gates.py` (a standing referee).
3. `experiments/exp61_epoch_baselines/README.md` — the retreat is a
   reporting decision (shared model at its class ceiling at z<=1.0); z=2 is
   a FORM problem (single-epoch score_A 0.835, +15.9%); M2 curvature a
   clean null.
4. `experiments/exp60_stochastic_layer/README.md` + `PROBLEMS.md` — the
   layer: anatomy (Rc must be ~20 kpc; the `c` axis), the cube, the
   held-out judgment, and **Option A ADOPTED as hongshao v1's stochastic
   layer** (frozen: `outputs/hongshao_v1_layer.npz`; battery with drawn
   populations: `figures/qa/qa_*_exp60_v1_layer.*`); **Option B on record**
   with revisit triggers (3-D/multi-axis data, or a reopened mean-model
   question).

## In Progress (Not Finished)

- Nothing running; nothing uncommitted. The only owed artifact is the exp61
  visualization (next session's task).
- A delegated Sonnet verification agent's final figure-check report never
  landed; the figures were verified directly instead — no action needed.

## Problems / Blockers

- **Local `master` ref is STALE at `a5e4f6a`**, held by the exp56 worktree
  (`hongshao_exp56_compact_shape`) — another agent's, do not touch. The
  session's work was pushed via `git push origin HEAD:master` from the
  `exp60-stochastic-layer` branch, whose head EQUALS origin/master
  (`85740a7`). When that worktree is released, plain `git checkout master
  && git merge --ff-only origin/master` reconciles it.
- An `exp62-cog-fit-atlas` worktree exists at `a5e4f6a` — possibly another
  active agent. `exp62` is TAKEN.
- Open questions C13 (the z=2 median needs a user scope decision before any
  re-attempt), C14 (the exchangeable-draw decliner-depth ceiling), C15 (the
  S2 plane levers) — filed in `doc/open_questions.md`, none blocking.

## Key Decisions (all the user's, all recorded in the READMEs and memory)

- Pinned-size X3 NOT adopted as mean; the old transport model's
  like-for-like numbers are the standing performance bar.
- The stochastic layer's **Option A adopted**; Option B on record.
- The z<=1.0 scope is a reporting statement, not a refit.
- The inner-region / high-z residuals await ADDITIONAL DATA (3-D or
  multi-axis profiles, the real merger tree) — the user's standing read.

## Branch State

- On `exp60-stochastic-layer` (== origin/master `85740a7`), tree clean,
  everything pushed. Chain branches exp57/58/59 deleted after merge; the
  merge union-resolved `doc/lessons.md` and `doc/todo.md` against exp56's
  parallel additions.

## Files Modified This Session

New experiments: `experiments/exp58_reassessment/`,
`experiments/exp59_conditioned_efficiency/`,
`experiments/exp60_stochastic_layer/`, `experiments/exp61_epoch_baselines/`
(scripts + READMEs + PROBLEMS committed; outputs/figures gitignored,
regenerable). Docs: `doc/plans/2026-08-26-exp58-reassessment-plan.md`,
`doc/plans/2026-08-26-exp60-stochastic-layer.md`, `doc/lessons.md`,
`doc/open_questions.md` (C13–C15), `doc/todo.md`, exp61 additions in
exp57-adjacent trees. Vault journal:
`wensai/development/hongshao/hongshao_master_2026-08-27.md`.

---
*Session: cbbdc336-ad1b-4dee-8122-bf90430b189e — resume with
`claude --resume cbbdc336-ad1b-4dee-8122-bf90430b189e`*
