# Session Handover — 2026-09-13 (exp82 adopted and merged; exp83 next)

## Goal
The hongshao mean model programme. This session: exp82 (the deposition
delay and the expansion exponent together, chosen by the gates on a tau_d
grid), its adoption as the baseline mean, and the wrap-up. The next session
starts **exp83 — roadmap S3, the early-mass conditioning of the centre on
the adopted exp82 mean** (`doc/plans/2026-09-12-roadmap-evidence-rethink.md`
§3 S3 and §5).

## Completed This Session
- exp82 grid (tau_d 0.10 / 0.15 / 0.20 / 0.30 × three starts, two fits at
  a time), per-point judges with every basin named, round 3 (exponential
  arrival at 0.15), the full judge with figures, the residual and decliner
  checks. Record: `experiments/exp82_delay_expansion/README.md`; figures
  `experiments/exp82_delay_expansion/figures/qa/*exp82_chosen_tau0.15*`
  (with `baseline_mean`, `wide_basin`, `frozen_point`, `old_14p63_basin`).
- **The adopted mean (the user, 2026-09-13)**: tau_d = 0.15 (step, held)
  + q_e = 0.152 (fitted) on exp63's twelve — 14 parameters, 13 fitted;
  loss 12.70 (previous mean 15.56); size offsets 14 of 15 + one width;
  the centre +0.5 / −6.2 / −6.9 per cent at z = 0.4 / 1.5 / 2; the
  formation-time residual gone. Pointer:
  `experiments/exp74_c19_history_leak/rebaseline.py::adopted_mean()` (dict
  with `spec`, `theta13`, `law`, `predict`); the previous mean stays as
  `adopted_baseline()` for every comparison. CLAUDE.md's BASELINE MEAN rule
  and `docs/SPEC.md` updated (commit `f1fa62d`).
- Merged to master `ff984b9`, pushed; branch deleted; gitignored outputs
  in place. Vault journal `development/hongshao/hongshao_master_2026-09-13.md`.

## In Progress (Not Finished)
- Nothing running. exp83 not started: its first step is the frozen probe
  — regress the adopted model's residual at z = 0.4 (5 and 103 kpc) and at
  z ≥ 1.5 on the early-mass fraction at fixed halo mass
  (`experiments/exp81_evidence_rethink/residual_check_model.py` with the
  exp82 file, then a binned-shape script like `residual_shape.py`), read
  the functional form, implement ONE early-mass term (candidates: the
  efficiency or the compact share rising with the mass fraction assembled
  by 2 Gyr; a `size_law` knob or `model2.compact_share` term), probe at
  frozen theta on the decliner split (exp81 measurement 1 §3), and fit
  only on a passing probe (three starts, two at a time).

## Problems / Blockers
- Costs carried with the adopted mean (C26): the mh-complete progenitors'
  50–100 kpc shell at z ≥ 1.5 −12 / −18 per cent; the future-dependence
  gate +0.03 dex per dex at z = 0.7 and z = 2; the z = 0.4 early-mass
  residual (+0.37 at 5 kpc), which is exp83's target.
- The stochastic layer is still not re-baselined (exp84 / S4).
- Machine: at most TWO delay fits at once (11.5 GB each); launch through
  `experiments/exp82_delay_expansion/launch.py` (own session) or
  `queue.sh`; the Obsidian CLI hung once after the reboots (write files
  directly).

## Key Decisions
- tau_d held on a grid and chosen by the gates (0.15), not fitted: the
  step arrival has no gradient and the loss's minimum (0.20) is the
  wide-split family that narrows the widths; the exponential form is
  equivalent and slightly behind.
- Adoption by the gates with the loss agreeing — the first such case;
  `adopted_baseline()` kept unchanged so every existing judge still runs.

## Branch State
- Branch: `master` at `ff984b9`, clean, pushed. exp83 is the next free id
  (check `git worktree list`, `git branch -a`, `ls experiments`; exp79 is
  Codex's).

## Files Modified This Session
- `experiments/exp82_delay_expansion/*` (new), `experiments/exp74_c19_history_leak/rebaseline.py`
  (`adopted_mean`), `experiments/exp80_deposit_size_law/stage1_{fit,eval}.py`
  (`--outdir`, `--delay-exp`, `--name`, …), `experiments/exp81_evidence_rethink/residual_check_model.py`,
  `CLAUDE.md`, `docs/SPEC.md`, `doc/lessons.md`, `doc/todo.md`, `doc/open_questions.md`,
  `doc/plans/2026-09-12-*.md`, `doc/journal/2026-09-13_exp82_handover.md`, this file.

---
*Session: 558e6e68-0029-4cae-9b46-e02020989b5e — resume with `claude --resume 558e6e68-0029-4cae-9b46-e02020989b5e`*
