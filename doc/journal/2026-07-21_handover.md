# Session Handover — 2026-07-21

## Goal
exp43 — the extrapolation ladder — is COMPLETE, recorded, and merged.
The user's framing: in observations low-z data is rich and high-z
scarce, so a kernel's upward-in-z extrapolation capability is a
first-class model-selection criterion for forward modeling. No task in
flight.

## Completed This Session (all merged to master `ea133b1` and pushed)
1. **exp43 — the extrapolation ladder**
   (`experiments/exp43_extrapolation/`, branch
   exp43-extrapolation-ladder): both kernels (1ch-mof, 2ch-exp) x five
   low-z fit scopes (z04 / z07 / z10 / z15 / z20; six rungs reused
   from exp38 stage3_single + exp40 latestart + exp38 stage2, four
   fitted new — 1ch-z07, 2ch-z07/z10/z15), everything unfitted judged
   (shape, inner masses, differential pairs; full n=2397).
2. **HEADLINE FINDINGS** (exp43 README, permanent record):
   - **Two epochs is the minimum viable fit scope.** z04-only fails
     both ways: 1ch collapses honestly (q unidentifiable -> 0;
     44% shape / -47% M(<5) at z=2; inverted differential trend);
     2ch posts the table's BEST extrapolated z=2 shape (12.7%*) with
     sign-flipped inner masses (+3..+8%) and a broken differential
     (0.54/0.20 vs data 0.37/0.11) — flexibility extrapolates the
     metric, not the physics.
   - **The second epoch locks the transport clock** (q: 1ch 0 -> 0.66
     -> 0.79 -> 0.91 monotone up the ladder; 2ch 1.41 -> 0.74 ->
     0.83) and the **1ch z07 fit predicts the unfitted high-z
     differential pairs at data precision** (z1.5->1.0* 0.27/0.08 vs
     data 0.27/0.07; z2.0->1.5* 0.23/0.06 EXACT — better than the
     five-epoch fit trained there, 0.18/0.05). The exp40
     "late-anchored transport extrapolates up" finding extends to a
     two-epoch anchor.
   - **The third epoch buys most of the remaining shape** (z=2.0*
     17.2% vs the five-epoch fit's own 14.1%); diminishing returns
     beyond.
   - **2ch-exp's high-z physics does not extrapolate at ANY scope**
     (z2.0->1.5 differential ~0.31-0.33 vs data 0.23, fitted or not)
     — a NEW extrapolation argument for the 1ch adoption.
3. **Demonstration figures** (`ladder.py figures`, user-requested):
   `exp43_differential` (measured curve vs every scope, unfitted pairs
   open), `exp43_residual_profiles` (148-pinned residual vs radius at
   z=1.5/2.0 — shows the 2ch z04 sign-flipped core instantly), the
   ladder overview `exp43_ladder`, and the STANDARD QA set for the
   star rung (`qa_*_exp43_1ch-mof_z07.*`, two-epoch fit evaluated at
   all five epochs).
4. **Records**: exp43 README results + figure pointers; doc/todo.md
   entry; doc/lessons.md +1 (judge extrapolation jointly on metric +
   physics + residual anatomy — a flexible model can extrapolate a
   metric while being wrong).
5. Commits: `7bed5b7` harness, `a9589a7` results, `7a77f69` figures,
   `a934bf7` docs, merge `ea133b1`. Earlier this same working session
   (2026-07-19 log): tech notes merged (`6b8fc46`), exp42 merged
   (`21bf53f`) — see `doc/journal/2026-07-19_handover.md`.

## In Progress (Not Finished)
- Nothing. Candidate next steps (no user direction yet):
  (a) **CV the z07/z10 rungs** if a production fit-scope decision is
  to be made on them (the natural follow-up named in the README);
  (b) a JAX port of the kernel (tech-note-2 §7 spells out
  feasibility); (c) tech-note iterations after user reading;
  (d) the user said "run more experiments to improve either the 1ch
  or the 2ch model" — extrapolation is now a measured axis to
  optimize against.

## Problems / Blockers
- None. Watch-outs unchanged: HONGSHAO_DATA_DIR export; nohup +
  caffeinate -im + disown for long fits (both exp42/43 chains ran
  cleanly); soft hyphens in written prose (perl check); /bin/rm in
  scripts; exp39-43 scripts path-load exp38 stage2/3 (spawn-safe
  _w_init, parent-side penalties).

## Key Decisions
- exp43 judged criteria were fixed before fitting (README); the
  2ch-exp z15 log_s0_ex=3.0 rail matches its recorded five-epoch
  behavior (stress-benign box clip) — noted, not re-stressed.
- The reused z04 rungs came from single-start fits (exp38
  stage3_single); README flags: refit with two starts if a z04
  conclusion ever becomes load-bearing.

## Branch State
- Branch: master, clean, pushed through `ea133b1`
  (exp43-extrapolation-ladder merged and deleted).
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit) and this
  handover (commit as the next branch's first commit).
- Gitignored outputs: exp43 `outputs/{ladder,ladder_report}.npz` +
  `run_full.log` + all `figures/*`.

## Files Modified This Session
- New: `experiments/exp43_extrapolation/{README.md, ladder.py}`,
  `doc/journal/2026-07-21_handover.md` (this file);
  `doc/journal/2026-07-19_handover.md` committed as the branch's
  first commit.
- Updated: `doc/todo.md` (+exp43), `doc/lessons.md` (+1), wensai
  daily note (snapshot).

---
*Session: 0a9faee5-d7e7-433b-a6ae-a040aa790cc2 — resume with
`claude --resume 0a9faee5-d7e7-433b-a6ae-a040aa790cc2`*
