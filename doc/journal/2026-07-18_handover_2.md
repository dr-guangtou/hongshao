# Session Handover — 2026-07-18 (session 2: exp40)

## Goal
The two user-planned scope tests (exp40) are COMPLETE and merged.
**Headline: z<=1.5 is now the kernel's OFFICIAL fit scope (user
adoption, 2026-07-18)**, with the z<=2.0 five-epoch fit retained as
the comparison option. Next session: no task in flight — the parked
stochastic layer (exp32 step 4) is the agreed eventual approach, or
whatever the user picks next.

## Completed This Session (all merged to master `0e57f0f` and pushed)
1. **Test 1 — late-start fits**
   (`experiments/exp40_epoch_objective/tests.py`, full n=2397): the
   adopted 1ch-mof kernel fit jointly from z<=1.5 and z<=1.0 instead
   of z<=2.0. Held-out M(<5) at z=0.4: -11% -> -9.5% -> -8.2% toward
   the -7.6% population-sharing wall — the multi-epoch transport tax
   is paid to the dissipative z>=1.5 era. The two late starts agree on
   every parameter; the physics IMPROVES (overshoot halved to T1
   +0.025/+0.028; all fitted differential pairs pass; the NEVER-FITTED
   z2.0->1.5 pair is predicted better than the z=2-anchored fit
   trained on it: 0.21-0.23 vs 0.18, data 0.23). Held-out shape on
   fitted epochs unchanged (18.2-16.4 vs 18.5-16.2); z=2 as pure
   extrapolation costs +1.2 shape points. z<=1.0 is too short a lever
   arm (z>=1.5 extrapolation degrades to -14/-16% inner).
2. **The g = 4.00 rail stress-tested benign** (`latestress`, box
   4 -> 6): settles interior at 4.37 for a 0.1% gain, physics
   unchanged — a flat ridge, NOT the exp38 pathological basin the
   value pattern-matches. New lesson in doc/lessons.md: a rail's
   meaning is scope-dependent.
3. **Test 2 — the re-aimed fiducial fully judged** (`reaim`,
   `reaimcv`): the exp38 capacity theta (12-param kernel under the
   inner-aware objective) reaches 14.9% avg held-out shape and M(<5)
   -6.4% — and FAILS the differential (0.48/0.15 vs data 0.37/0.11)
   with mu railed and the overshoot back. exp39's objective-driven
   re-balancing confirmed with zero added components. The inner-aware
   objective stays an evaluation metric, NOT a fitting loss.
4. **ADOPTION recorded** (exp40 README + doc/todo.md): official
   theta = exp40 `outputs/latestart.npz[theta_z15]` (params
   [2.74, 4.00, 0.91, 1.52, 0.24, 1.38]); comparison = exp38
   `outputs/stage2_multiepoch.npz[theta_1ch-mof]`.
5. Docs: exp40 README (all tables + adoption), todo (exp40 entry +
   adoption entry), lessons (+1), wensai journal
   `hongshao_master_2026-07-18_2.md`.

## In Progress (Not Finished)
- Nothing in flight. Standing candidates: the exp32 step-4 stochastic
  layer (user-parked; the agreed eventual approach, doubly motivated
  by exp39 lead 3 — correlated theta-deviations in the anatomy
  subspace, target centered plane energy -> ~1x floor; first step
  would be re-running the anatomy decomposition on the CURRENT kernel
  and checking alignment with the exp39 per-galaxy f_core direction);
  exp37 product-side work; propagating the new official scope to any
  downstream kernel use.

## Problems / Blockers
- None. Watch-outs: (1) `export
  HONGSHAO_DATA_DIR=/Users/shuang/Desktop/tng300_mah_mprof`; (2) long
  fits via nohup + caffeinate -im + disown with a log tail; (3) the
  g=4.0-rail watch item is now NUANCED — in the z<=1.5 scope it is
  stress-verified benign; in the 5-epoch scope it still marks the bad
  basin; (4) exp40 tests.py path-loads exp38 stage3 (spawn-safe
  _w_init, parent-side penalties — the stress test relies on
  parent-side e.HI override); (5) `rm` aliased to Trash-move, fails on
  missing files; (6) never commit doc/CLAUDE.md.

## Key Decisions
- **z<=1.5 ADOPTED as the kernel's official fit scope; z<=2.0 kept as
  the comparison option (user).** Physical reading: z>=1.5 mergers are
  dissipative, outside the additive collisionless model's domain — the
  fit scope is the physical statement; the model is judged by
  extrapolation into the dissipative era.
- The inner-aware objective is confirmed non-adoptable for the
  fiducial fit (test 2); it remains the evaluation metric for inner
  masses.
- The remaining official-scope inner deficit (-9.5% held-out) is
  dominated by the -7.6% sharing wall = per-object information
  (exp39 lead 3) -> the stochastic layer, not more conditioning.

## Branch State
- Branch: master, clean, pushed through `0e57f0f`
  (exp40-epoch-objective merged and deleted).
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit) and this
  handover file (commit as the next branch's first commit).
- Gitignored outputs: exp40 `outputs/*.npz` (latestart, latestress,
  cv_z15, cv_z10, cv_reaim) + 3 run logs.

## Files Modified This Session
- New: `experiments/exp40_epoch_objective/{README.md, tests.py}`,
  `doc/journal/2026-07-18_handover.md` (committed, exp39 session),
  `doc/journal/2026-07-18_handover_2.md` (this file).
- Updated: `doc/todo.md` (exp40 + adoption entries), `doc/lessons.md`
  (+1), wensai journal x2 + snapshots.

---
*Session: f1d741b1-9def-45a0-843b-46cee79308d1 — resume with
`claude --resume f1d741b1-9def-45a0-843b-46cee79308d1`*
