# Session Handover — 2026-07-19

## Goal
Two deliverables, both COMPLETE and merged: (1) the journal-style
technical documentation of the two emulators (`doc/tech_note/`), and
(2) exp42 — the real-MAH head-to-head for the adopted 1ch-mof kernel,
closing the tech-note-2 caveat. No task in flight.

## Completed This Session (all merged to master `21bf53f` and pushed)
1. **`doc/tech_note/` — the tech notes** (branch tech-note-emulators,
   merge `6b8fc46`): README index + three Markdown+LaTeX notes —
   01 the statistical emulator (heteroscedastic conditional Gaussian
   core, PCA profile modes, exp37 multi-epoch AR(1) block-pinned
   product), 02 the adopted 1ch-mof kernel at the official z<=1.5
   scope + the exp41 stochastic layer + a differentiability/real-MAH
   section (§7), 03 the 2ch-exp alternative and the supersession
   verdict. 12 pedagogical figures + 2 GIF animations
   (`note2_assembly.gif` = deposition-then-migration flagship), all
   generated by `doc/tech_note/scripts/` (self-contained kernel
   mirror, self-checked). PDF export: `scripts/build_pdf.sh` (pandoc +
   xelatex; output `doc/tech_note/pdf/` gitignored). QA-set pointers
   in every note + a central table in the index.
2. **exp42 — the real-MAH comparison** (branch exp42-real-mah-kernel,
   merge `21bf53f`; `experiments/exp42_real_mah/`): the SAME 12-param
   1ch-mof + z<=1.5 protocol with config="real" (de-dipped peak
   history, keeps merger bursts). **HEADLINE: the Gaussian-era
   3-5-point real-MAH penalty does NOT survive the Moffat tail** —
   held-out shape ties at z<=1.0, WINS at z>=1.5 (18.1/17.1/16.7/15.5,
   z=2.0* 13.6 vs DiffMAH 15.4; M(<5) -8.9 vs -9.5 at z=0.4), from a
   fully bound-free basin [log_rc 1.56, g 2.43, q 0.79, mu 1.78
   (peak z=4.9), sig 0.52, gamma 1.28] — the g=4.0 rail was a property
   of the SMOOTH INPUT BASIS, not the model. Physics mixed: exact
   0.37/0.11 on the flagship z0.7->0.4 differential (best-ever), but
   earlier pairs undershoot (0.26/0.21/0.16 vs 0.36/0.27/0.23) and the
   low-mass 60-148 kpc overshoot hits +0.080 dex (out of band).
   Transfer cross: the official theta unrefit on real input loses 3.6
   shape points — the inputs demand different transport configs.
3. **USER DECISION (2026-07-19): the real-MAH config does NOT become
   the default** — DiffMAH stays the operating point (physics band +
   differentiability + the exp41 layer's calibration); the real input
   is recorded as an accuracy PEER, not a fallback. Standard QA set
   generated on the held-out CV curves
   (`figures/qa_*_exp42_real_z15.*`, `compare.py report`).
4. Records: exp42 README results + decision; tech-note-2 §7 caveat
   replaced with the measured result; `doc/lessons.md` +1 (an input
   verdict can be a property of the old model form — re-test when the
   form changes); `doc/todo.md` exp42 + tech-note entries.

## In Progress (Not Finished)
- Nothing. Candidate next steps (no user direction yet): (a) a JAX
  port of the kernel (tech-note-2 §7 spells out feasibility); (b) if
  the real-MAH basin is ever revived, the exp41 stochastic layer must
  be RECALIBRATED on it (the deviation pool is measured around the
  DiffMAH optimum) and the outskirt band re-judged; (c) tech-note
  wording/figure iterations after user reading.

## Problems / Blockers
- None. Watch-outs: (1) `export
  HONGSHAO_DATA_DIR=/Users/shuang/Desktop/tng300_mah_mprof`; (2) long
  fits via nohup + caffeinate -im + disown (worked cleanly for the
  ~1 h exp42 chain); (3) soft hyphens (U+00AD) sneak into written
  prose — check with perl, BSD grep misses them; (4) `rm` is aliased
  to Trash-move — use /bin/rm in scripts; (5) pandoc 2.12 + xelatex
  on this machine for the tech-note PDF build; (6) exp42 `compare.py`
  re-inits the exp38 stage-2 state with config="real" (spawn-safe
  `_w_init`, parent-side penalties — the exp39-42 pattern).

## Key Decisions
- **DiffMAH stays the kernel's input configuration** (user): the real
  input now ties/wins on held-out accuracy but strains the outskirt
  physics band and forfeits differentiability; verdicts recorded as
  CONDITIONAL on the deposit form (the new lesson).
- Tech-note format/staging as decided 2026-07-18 (Markdown + LaTeX,
  three notes, pedagogy over QA) — delivered; PDFs are derived
  artifacts (gitignored), rebuilt by script.

## Branch State
- Branch: master, clean, pushed through `21bf53f` (tech-note-emulators
  and exp42-real-mah-kernel both merged and deleted).
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit) and this
  handover (commit as the next branch's first commit).
- Gitignored outputs: exp42 `outputs/{realfit,cv_real}.npz` +
  `run_full.log` + `figures/qa_*_exp42_real_z15.*`;
  `doc/tech_note/pdf/`.

## Files Modified This Session
- New: `doc/tech_note/` (4 md + 14 committed figures + 4 scripts +
  build_pdf.sh), `experiments/exp42_real_mah/{README.md, compare.py}`,
  `doc/journal/2026-07-18_handover_3.md` (committed),
  `doc/journal/2026-07-19_handover.md` (this file).
- Updated: `doc/todo.md` (+2 entries), `doc/lessons.md` (+1),
  `.gitignore` (+doc/tech_note/pdf/), wensai daily note (snapshot).

---
*Session: 0a9faee5-d7e7-433b-a6ae-a040aa790cc2 — resume with
`claude --resume 0a9faee5-d7e7-433b-a6ae-a040aa790cc2`*
