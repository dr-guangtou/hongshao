# Session Handover — 2026-07-18

## Goal
The core-channel revisit (exp39) is COMPLETE and merged. All three
planned leads plus the user-approved retention follow-up were measured;
the core channel stays PARKED by user decision, now backed by a closed
factorial. **Next session: discuss the next direction with the user —
the session ended at "back to master, then let's discuss what should be
the next step."**

## Completed This Session (all merged to master `735464b` and pushed)
1. **exp39 lead 1 — the six-form core shootout**
   (`experiments/exp39_core_revisit/shootout.py`, full n=2397):
   inherited-Moffat / Gaussian / exponential / Sersic n=2-3-4 cores at
   a common half-mass-radius parameterization (box 0.5-8 kpc). All six
   fit to the same inner-aware loss (0.2540-0.2543) and ALL fail the
   differential-deposition test identically (0.53-0.56/0.18-0.19 vs
   data 0.37/0.11). **The exp38 post-mortem's tail-leakage mechanism is
   FALSIFIED** — a zero-leakage Gaussian core breaks the physics
   exactly like the 8%-leaky Moffat. The breaker is the freed outer
   kernel re-balancing (wider late deposits) whenever anything absorbs
   the inner mass.
2. **The frozen-kernel probe**: kernel pinned at the adopted stage-2
   theta, only the 3 core params fit. Physics stays at the kernel's own
   pass (0.38-0.39/0.12); held-out M(<5) -11 -> -4.4%, M(<10) -5.4 ->
   -1.0% at z=0.4; shape avg 16.1-16.5% vs kernel 16.6%. **User
   decision: NOT graduated** — pinning is an ad-hoc restriction, not a
   mechanism; the joint optimum of the same structure breaks physics.
3. **Lead 2 — z=2 halo-mass conditioning** (`--cond z2`,
   `logmh_zk_real[:, 4]`): helps at the frozen point (0.2737 -> 0.2719,
   better inner bias both ends), no gain at the free point (rails mu).
4. **Lead 3 — per-galaxy core diversity** (`percore`): f_core spans
   0.00-0.20 (1.45 dex logit scatter), closes the shared median M(<5)
   to +2.1%, but best feature correlation |rho| = 0.31 — per-object
   information; only a statistical scatter layer could carry it.
5. **The retention x inner-aware cell (user-approved final task)**: the
   exp38 factorial hole. Global floor f_ret = 0.564, NO bound, best
   held-out shape in the program (16.2/16.7/16.1/13.7/11.1%, avg 14.8%)
   and the only z=2 inner gain (M(<5) -2.3%) — yet the SAME differential
   failure (0.53/0.17; outskirt T1 +0.065/+0.099 held-out). The
   boundness-conditioned floor (f_ret,i on log10(rc0/R200), virial
   radii from the measured halo mass history) is a measured null
   (b = 0.10) that rails mu.
6. Docs: exp39 README (full tables + FINAL DECISION), doc/todo.md
   (both exp39 entries closed), doc/lessons.md (+1 completed lesson),
   wensai journal `hongshao_master_2026-07-18.md`.

## In Progress (Not Finished)
- Nothing in flight. The next step is a DISCUSSION: what to work on
  next. Candidate directions the record supports (not user-chosen yet):
  (a) the exp32 step-4 stochastic layer (PARKED 2026-07-11 entry in
  todo) — per-object scatter layers are now doubly motivated by exp39
  lead 3; (b) a different kernel family / consistency constraint if the
  inner deficit is ever re-attacked (the exp39 closing statement);
  (c) product-side work on the graduated exp37 multi-epoch emulator.

## Problems / Blockers
- None. Watch-outs unchanged: (1) `export
  HONGSHAO_DATA_DIR=/Users/shuang/Desktop/tng300_mah_mprof` every run;
  (2) long fits via nohup + caffeinate -im + disown with a log tail;
  (3) exp39 shootout.py path-loads exp38 stage3 (spawn-safe
  `_w_init(rows, cond_key)`, penalties parent-side); (4) `rm` is
  aliased to a Trash-move and fails on missing files (breaks `&&`
  chains); (5) never commit doc/CLAUDE.md.

## Key Decisions
- **Core channel stays PARKED; nothing graduates (user, 2026-07-17).**
  The frozen-kernel fit rejected as "cheating": no physical mechanism,
  improvement exists only under the artificial freeze.
- **The factorial is closed**: {6 core forms} + {2 retention modes}
  under the inner-aware objective all fail the physics in the same
  band. The re-balancing is OBJECTIVE-driven; the inner-mass /
  outskirt-physics tension is structural to the kernel family. Any
  future attack = different kernel family or consistency constraint,
  not another inner-mass allocation mechanism.
- The adopted kernel remains exp38 1ch-mof (12 params) unchanged;
  observation-facing inner masses remain the statistical emulator's
  product (exp37, M(<10) bias 1-2%).

## Branch State
- Branch: master, clean, pushed through `735464b`
  (exp39-core-revisit merged and deleted).
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit).
- Gitignored outputs in place: exp39 `outputs/*.npz` (shootout, frozen,
  frozen_z2, retention, percore, all cv_* files) + 5 run logs.

## Files Modified This Session
- New: `experiments/exp39_core_revisit/{README.md, shootout.py}`,
  `doc/journal/2026-07-16_handover.md` (committed),
  `doc/journal/2026-07-18_handover.md` (this file).
- Updated: `doc/todo.md` (exp39 entries), `doc/lessons.md` (+1),
  wensai journal + snapshot.

---
*Session: f1d741b1-9def-45a0-843b-46cee79308d1 — resume with
`claude --resume f1d741b1-9def-45a0-843b-46cee79308d1`*
