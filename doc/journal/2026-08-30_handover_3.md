# Session Handover — 2026-08-30 (exp63 Stage 5 closed, merged and pushed)

*(The third handover written today: `2026-08-30_handover.md` and `_2.md` belong to
the exp62 and exp67 sessions that were merged into `master` in parallel with this
one. For exp63's mid-round state see `2026-08-28_stage5_handover.md`, which this
one closes.)*

## Goal

Close the exp63 branch: finish the single-epoch round, fix the fitting-sample gap
the user identified, choose the branch's product, merge and push, and set up the
next session — whose **first job is a discussion, not a new experiment**.

## Completed This Session

- **Stage 5a–5c** (z=0.4 only, the user's direction): `single_epoch.py` (the
  visual gate + the lever sweep by evaluation) and the `stage2_fit.py` flags
  `--freeze / --levers / --extended-family / --objective / --compact-kpc / --tag`.
  The production per-galaxy loss is nearly blind to the tercile-median offset the
  gate reads (0.18 dex per-galaxy scatter against a 0.065 dex offset at 2–5 kpc in
  the top halo-mass tercile), so `--objective binned` prices it; the structural
  fix is a **compact channel sized in physical kpc** rather than as a fraction of
  R200c at the deposit — every halo-mass tercile within ±3 % at every radius.
- **Stage 5d–5e**: the extrapolation of the two z=0.4 candidates, then each epoch
  fitted alone (`--epoch k`). The kpc form fits every epoch to gate rms 1.05–1.50
  with smoothly moving parameters.
- **Stage 5f**: the joint five-epoch fit (`--joint`, one θ, the binned objective
  summed over epochs, per-epoch masks/σ_A/terciles/null references). Better than
  the five-epoch incumbent at every epoch; short of the per-epoch ceiling; z=2
  fails in amplitude; every joint start rails `n_c` at 0.5 — diagnosed as the
  shared law misusing the compact channel, not a physical preference.
- **Stage 5g**: `history_outliers.py` — 41 galaxies more than 0.5 dex off the
  running-median M*–Mh relation at some epoch (a minor or wrong progenitor early,
  never over-massive).
- **Stage 5h**: exp62's cubic-logit and double-Sérsic families read and assessed:
  not a kernel change for the deposition model; a candidate *objective*.
- **Stage 5i — THE FITTING SAMPLE (the user's repo-wide rule)**: every fit had
  been using exp54's per-epoch mh-complete masks as the *fit* sample, against
  exp54's own design. The rule is now `selection.fitting_sample_mask` (all
  z=0.4-selected galaxies with a sane halo and stellar history at every epoch —
  2356 of 2397; halo-mass completeness is an after-fit check), stated in the
  repo-root `CLAUDE.md` and `docs/SPEC.md`, with `--sample sane` the default.
  Refits: each epoch alone 1.42 / 1.17 / 1.37 / 1.23 / 2.86 on the fitting sample
  (the old-mask fits judged there: 1.46 / 1.56 / 2.93 / 3.54 / 11.91); the joint θ
  3.33 / 2.36 / 3.57 / 3.28 / 6.88 (incumbent 5.04 / 6.25 / 6.86 / 4.99 / 8.45).
  On the mh-complete check every refit is worse than the old fit that had been
  tuned to it — a z=2 halo-mass dependence the old masks had hidden.
- **Branch product (the user)**: `outputs/stage2_fit_joint_kpc_free_sane.npz` is
  the exp63 mean, and is explicitly **not** proposed as hongshao's adopted model;
  the README's "The branch's product decision" says what stands against adoption.
- **Merged and pushed**: `master` (exp62 + cubic-logit, exp65–67) merged into the
  branch (`3315e3c`, both-sides append conflicts in `doc/lessons.md` and
  `doc/todo.md`, both kept); the branch merged into `master` with `--no-ff`
  (`0dfb879`) from the integration worktree and pushed to `origin/master`.
- Records: exp63 README Stages 5a–5i + product decision + closing summary;
  `doc/lessons.md` (the single-epoch round's lessons); `doc/open_questions.md`
  C17 and C18; `doc/todo.md`; memory `loss-is-blind-to-the-binned-gate` and
  `fitting-sample-rule`; vault journal
  `~/work/wensai/wensai/development/hongshao/hongshao_exp63-analytic-growth_2026-08-30.md`.

## In Progress (Not Finished)

- Nothing is running and nothing is uncommitted. **The next session's first job
  is to DISCUSS, not to create an experiment** (the user), covering:
  1. **C17 — a likelihood with a mock CoG error model** instead of the bare
     distance objectives used throughout (Poisson from particle counts, a
     softening floor at the inner apertures, a projection/orientation term, and
     the radius-to-radius correlation a cumulative profile necessarily has).
     Would it make the fits better behaved — fewer railed parameters, no need to
     tell the loss by hand that the binned median matters? What must be
     calibrated before such a model is trusted?
  2. **C18 — the z=2 halo-mass dependence**, settled without any fit by the
     future-growth leakage test (residual against
     `log Mh(z=0.4) − log Mh(z=2)` at fixed `log Mh(z=2)`).
  3. **Whether the efficiency law's time dependence is the next model step**
     (Stage 5f: z=2 fails in amplitude, not shape).
- Left open for future experiments (the user): the cubic-logit size-and-shape
  objective test (README 5h), P9 (the split predicts the wrong sign of every
  assembly correlation), P10 (mean- vs median-preserving noise).
- If the joint mean is ever carried forward, the Stage 3 noise process and the
  Stage 4 battery must be refitted on it under the sample rule; they were built
  on the Stage 2 mean and the pre-5i masks.

## Problems / Blockers

- **Worktree layout**: the main worktree
  (`/Users/shuang/Dropbox/work/project/massive/hongshao`) is checked out on
  `exp63-analytic-growth`, while `master` lives in the
  `hongshao_exp62_cog_fit_atlas` worktree. The branch was therefore *not* deleted
  after the merge (git refuses to delete a branch a worktree is on). This is the
  same "worktree squatting on master" pattern that was fixed once for exp56;
  worth rearranging deliberately, not as a side effect of a merge.
- The `--physical` flag in `single_epoch.py` / `stage2_fit.py` is now redundant
  (the sane sample supersedes it) and could be removed.
- Pre-5i numbers in the exp63 README were fitted on the old masks; they are
  labelled as such and were deliberately not re-derived — they are the record of
  the drift.

## Key Decisions

- The fitting sample rule (the user, repo-wide): fit on all sane galaxies;
  halo-mass completeness is an after-fit check with θ frozen.
- The branch mean is the joint, four-exponent, kpc-compact fit on the sane
  sample; a product decision, not an adoption decision.
- `--objective binned` is legitimate: when the user's judgement is a binned
  average, that statistic belongs in the objective, with the production loss
  reported alongside every product.
- `n_c` railing at 0.5 is treated as a symptom (the shared law paying at a bound
  for a time dependence it cannot write), diagnosed by measuring what the channel
  became rather than by widening the bound.

## Branch State

- Branch: `exp63-analytic-growth` at `3315e3c`; working tree clean; no background
  jobs.
- `master` at `0dfb879` (contains this branch), level with `origin/master`,
  checked out in the `hongshao_exp62_cog_fit_atlas` worktree.
- Relationship: fully merged into `master`; the branch is kept only because the
  main worktree is on it.

## Files Modified This Session

New: `experiments/exp63_analytic_growth/{single_epoch,history_outliers}.py`,
`CLAUDE.md`, `doc/journal/2026-08-28_stage5_handover.md`, this file.
Modified: `experiments/exp63_analytic_growth/{stage2_fit,model2}.py` and its
`README.md`, `experiments/exp54_unpinned_amplitude/selection.py`, `docs/SPEC.md`,
`doc/{lessons,todo,open_questions}.md`. Figures/outputs are gitignored and
regenerable (`figures/qa_joint/`, `figures/qa_stage5/`,
`figures/exp63_single_epoch_*`, `figures/exp63_history_outliers.png`).
Memory: `fitting-sample-rule.md`, `loss-is-blind-to-the-binned-gate.md`, index.

---
*Session: d159bb71-e9de-4858-b20f-20a4763b8018 — resume with `claude --resume d159bb71-e9de-4858-b20f-20a4763b8018`*
