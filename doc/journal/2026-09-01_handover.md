# Session Handover — 2026-09-01 (C18 closed, C17 answered, C19/C20/C21 opened)

## Goal

The user's plan for the session, in order: **(1) C18** — is the z = 2 halo-mass
dependence the z = 0.4 progenitor selection? No fit required. **(2) C17 if C18
concluded smoothly** — what does having a measured curve-of-growth error do to
the fitting process, with the user's constraint that **at z = 0.4 the isophotal
error is used alone, no projection term**. **(3) Test different objective
definitions with the error information.**

All three were done. The session then extended twice on the user's reading of
the results, which produced the two open questions that matter most for the next
session (C20, C21) and a written plan for exp73.

## Completed This Session

Eight commits on `exp71-c18-selection-leakage`, none merged.

### exp71 — C18 CLOSED (`e896737`)

`experiments/exp71_c18_selection_leakage/{leakage.py,channel_origin.py,README.md}`.
No fit anywhere; theta frozen at exp63's joint five-epoch mean, with the nested
incumbent carried through every table as the control.

**The answer is yes, from 10 kpc outwards.** The z = 2 halo-mass tilt of
`M*(<103 kpc)` is −0.122 [−0.142, −0.101] on the fitting sample and **+0.061**
[+0.022, +0.097] on the mh-complete subset — opposite signs. The completeness
curve and the measured `dy/dG` predict a spurious tilt of −0.063…−0.180 with no
reference to the observed one; the observed −0.122 sits inside it, and
subtracting it leaves +0.058…−0.059, bracketing the mh-complete +0.061. The
leakage channel (partial ρ +0.180/+0.198/+0.207/+0.191 at z = 0.7–2.0) is outside
a within-mass-bin shuffle null at every epoch and is identical for the exp63 fit
and the nested incumbent. **Exception: `M*(<2 kpc)`**, where the shift is −0.092
and selection predicts at most −0.026 — the standing central defect.

**Part 2 found what C18 did not ask (→ C19).** TNG300's own `M*(<103 kpc)` at
fixed halo mass barely reads future growth (+0.003 dex/dex at z = 2); the model
reads it at +0.125. Future growth at fixed halo mass is predicted with
**R² = 0.00** from the MEASURED pre-z_k history (raw snapshots *and* a same-order
polynomial through every usable one — the control for the smoothing objection)
and **R² = 0.50 / 0.69 / 0.66 / 0.52** from the DiffMAH curve read at the same
times, **0.79 from `logmp` alone at z = 2**. DiffMAH's `logmp` is the halo's peak
mass and enters the curve at every time, so the model's "past" carries the
halo's final mass.

### exp72 — C17 ANSWERED (`ae6da8f`, `a28ac4d`, `d79cfea`, `1ab5b8e`, `efd857d`, `2ac3fa8`)

`experiments/exp72_error_objective/{errors.py,fit_gls.py,eval_gls.py,radial_evidence.py,README.md}`.

**Step 1, no fit** (`errors.py`, six selftest claims pass). Swept five candidate
objectives at frozen theta and disqualified four before spending a fit:

- **unfloored GLS is unusable** — at z = 0.4 *one galaxy* carried 100 % of the
  population's loss. `annulus_sigma_abs` spans eight decades (2 M⊙ against a
  median 3×10⁸) because past a galaxy's last isophote the CoG stops rising and
  its variance stops with it. Flooring at a fraction `f` of the cumulative error
  repairs it; f = 0.03–0.10 work, 0.30 over-smooths.
- **`chi2_diag`** — 2–5× more concentrated than the production objective at every
  floor; **`frac_sigw`** — moves the amplitude/shape ratio from 26 to 41 at z = 2,
  the wrong direction.
- **Cleared: `chi2_gls_amp` floored at f ≈ 0.1.**

Two findings worth more than the objective comparison: **every incumbent
objective becomes an amplitude meter at high z** (`prod`'s amplitude/shape ratio
5.1 → 26.4, its shape sensitivity collapsing to 0.2 %), and at z = 2 **only 67 %**
of galaxies have a positive measured error beyond 103 kpc.

**Step 2, the refit** (`fit_gls.py`, 8 starts, 2.3 h). Settled — all 8 land on
loss 4.271534 from starts spanning 4.59–18.28, theta agreeing to 0.0014. `n_c`
rails at its bound in all eight, reproducing exp63's Stage 5f pathology.
Transfer matrix: wins its own objective by 7.0 %, loses exp63's by 21.3 %,
landing at 18.59 against the *unfitted* incumbent's 19.24.

**Step 3, the radial evidence** (`radial_evidence.py`), run at the user's
prompting — see "Key Decisions".

## In Progress (Not Finished)

Nothing is running. One file is uncommitted: the exp73 plan's decision section
(`doc/plans/2026-09-01-exp73-size-relative-objective.md`) — commit it first
thing.

**The next session's work is specified and written up in that plan.** Stage 1 is
evaluation-only and needs no new machinery beyond a size-relative coordinate.

## Problems / Blockers

- **Nothing is merged.** The branch is 8 commits ahead of `master` and the user
  has not given merge permission. `master` lives in the `hongshao_master`
  worktree.
- **The floor `f = 0.1`** on the annulus error is a modelling choice, not a
  measurement, and every exp72 Step 2 number is conditional on it.
- **The error model is knowingly too small** — isophotal only, no projection
  term (the user's constraint): reduced χ² 3.4 rather than 1.1 at z = 0.4.
- **C19 needs a fit** and touches every high-redshift number in the programme.
  Not started.
- `map_tng100_hist_stellar.hdf5` is still damaged; unchanged from 2026-08-31.

## Key Decisions

### Two corrections I made to my own reporting, both of which changed the reading

1. **I led with `M*(50–100 kpc)` at +42 % at z = 2 and called the outskirts
   wrecked.** The user checked the figure and disagreed. A shell is the
   difference of two large cumulatives and amplifies: the cumulative profile at
   103 kpc is −5.9 % against the incumbent's −4.6 %, a 1.3-point difference. The
   real cost was the high-z **centre** (−27.4 % against −11.0 % at 2 kpc).
   Also: the two QA panels use ±40 % and ±20 % y-axes, so the worse model looks
   calmer. → memory `quote-cumulative-before-a-shell`.
2. **I then over-claimed the opposite.** Having found the χ² fit puts the z = 0.4
   centre right (−2.1 % inside 2 kpc against the incumbent's +5.8 %), I called it
   "the first thing ever to move the central defect". **The control refutes
   that**: exp63's own objective fitted at z = 0.4 ALONE
   (`stage2_fit_frozen_binned_kpc_sane_e0.npz`, which already existed) reaches
   **−0.1 %**, better, with no error bars involved. The χ² was reallocating
   attention toward z = 0.4, behaving like a partial single-epoch fit. **Claim
   withdrawn** in `efd857d`.

### The user's three decisions (2026-09-01), recorded in the exp73 plan

- **D1** — `cog_provided` stays the fitting target. The **mass–size relation and
  the R20/R50/R80 fractional-size DISTRIBUTIONS at fixed stellar or halo mass**
  become a major validation/QA target. `qa.py` has R50/R80/R90; **R20 is owed**,
  as is scoring the distribution rather than the median, promoted to a numbered
  gate.
- **D2** — test both down-weighting schemes head to head: **A**, smooth
  mass-fraction weights (no free parameter) against **B**, a threshold gate (one
  parameter `t`). Rule fixed before any number is seen: **if B's verdict moves
  materially as `t` is swept over 0.5/1/2/4 %, B is disqualified in favour of A.**
- **D3** — **keep the shared law; drop the z = 2 per-epoch number as a target.**
  exp63 Stage 5i point 3 found its per-epoch parameter sequence BREAKS at z = 2
  and concluded the time law was "partly a mass-selection law"; exp71 then proved
  that reading right; exp61 showed per-epoch fits overshoot the population to
  +12.4 % at 100 kpc. **But the z ≤ 1 half of the gap is real and unexplained**
  (at z = 0.4, joint 3.33 against per-epoch 1.42, where no selection acts), and
  the untested lever there is **a richer time law for the two SIZE parameters** —
  register `D2` closed richer time dependence for the EFFICIENCY law only, on the
  pre-exp63 seven-parameter model.

### The evidence that decides exp73

- The galaxy shrinks by **3.9×** (median R50 11.6 → 3.0 kpc) and the grid does
  not move: 148 kpc is 13 R50 at z = 0.4 and **49 R50** at z = 2.
- At z = 2, **96.7 %** of the galaxy is inside 52 kpc and the outer two shells
  hold **3.3 %** of its mass — yet the production objective spends **27.8 %** of
  its attention there, an **eight-fold** over-weighting, while the 2–5 kpc shell
  holds 27.2 % and gets 17.7 %.
- The relative residual amplifies exactly there: the χ² refit at z = 2 is
  **+81.1 %** wrong at 103–148 kpc while misplacing **0.53 %** of the galaxy's
  mass, and +32.2 % wrong at 10–23 kpc while misplacing **3.00 %**.
- **In R50 units the galaxy is nearly self-similar**: shell mass fractions vary
  by 0.8–3.7 percentage points across the five epochs against up to 12.4 in fixed
  kpc.
- **The incumbent objective already holds the mass–size relation to 5 %** at
  every epoch (R50 error +0/+1/−1/+3/+5 %); the χ² breaks it, making z = 2
  galaxies **27 % too big**. That one number is the physical statement of
  everything else the χ² got wrong.

## Branch State

- Branch **`exp71-c18-selection-leakage`**, 8 commits ahead of `master`, NOT
  merged, NOT pushed. One uncommitted file (the exp73 plan's decision section).
- `master` at `4715e9e` in the `hongshao_master` worktree.

## Files Modified This Session

New: `experiments/exp71_c18_selection_leakage/{leakage.py,channel_origin.py,README.md}`,
`experiments/exp72_error_objective/{errors.py,fit_gls.py,eval_gls.py,radial_evidence.py,README.md}`,
`doc/plans/2026-09-01-exp73-size-relative-objective.md`, this file.
Modified: `doc/open_questions.md` (C18 resolved in place; C19, C20, C21 added),
`doc/lessons.md` (14 lessons), `doc/todo.md`.
Memory: `chi2-objective-nearly-solved-z04-centre.md`,
`quote-cumulative-before-a-shell.md`, `MEMORY.md`.
Gitignored products: `experiments/exp7{1,2}_*/outputs/*`, `.../figures/*`.

---
*Session: b6a70f11-aba0-4d8f-8373-21c630bb7956 — resume with `claude --resume b6a70f11-aba0-4d8f-8373-21c630bb7956`*

---

# Appended — end of session (merged, pushed, next session planned)

## What changed after the first handover was written

- **Merged and pushed.** `exp71-c18-selection-leakage` merged into `master` with
  `--no-ff` at **`5d142f4`** from the `hongshao_master` worktree (the main
  worktree cannot check out `master`), and pushed to `origin/master`. Ten
  commits, 4033 insertions, 13 files. The merged branch was **deleted**.
- **The next branch is cut and checked out**: **`exp73-size-relative-objective`**
  in the main worktree, tracking `origin/master`, clean. Nothing is squatting on
  `master`.
- **The user's three decisions were recorded** in
  `doc/plans/2026-09-01-exp73-size-relative-objective.md` as D1 / D2 / D3 — see
  "Key Decisions" above.
- **A 6–7 hour continuous schedule was written**:
  `doc/plans/2026-09-01-exp73-continuous-session.md`.

## The next session runs unsupervised — how

`doc/plans/2026-09-01-exp73-continuous-session.md` is a block schedule in which
**every block ends in a gate with a numeric threshold fixed in advance**, so the
session can proceed without asking. Summary:

| block | what | fit? |
|---|---|---|
| **A** (0:00–1:15) | build the size-relative coordinate, then **the risk test** | no |
| **B** (1:15–2:30) | the two down-weighting schemes head to head (D2) | no |
| **C** (2:30–5:00) | one refit under the winner, background | yes, ~2.5 h |
| **D** (parallel with C) | the QA additions D1 asks for: R20, size DISTRIBUTIONS at fixed mass, promoted to a numbered gate | no |
| **E** (5:00–6:15) | the richer size-time law (D3's untested lever), judged at z ≤ 1.0 | maybe |
| **F** (6:15–7:00) | consolidate, commit, handover. **Do not merge.** | no |

**Block A's risk test can cancel Blocks B–D**, deliberately. It asks whether the
joint-versus-per-epoch gap narrows under the new coordinate, and whether the
z ≤ 1.5 per-epoch optima lie on a smooth curve in time. If the gap does not
narrow and the optima are smooth, **the coordinate is not the limit and the time
law is** — skip to Block E and record that exp73's premise was tested and did not
survive. That is a result, not a failure.

**Stop and wake the user if**: a result contradicts the record (C18's answer, the
mass–size relation holding to 5 per cent, the fitting-sample rule); a gate is
passed only by moving the gate; anything suggests a published number is wrong; or
Block E's fit rails a parameter exp63 did not, or lands in more than one basin.

**C19 is deliberately NOT in the schedule.** It needs a fit, it touches every
high-redshift number in the programme, and it deserves the user's attention
before it is spent.

## What "improve the model" honestly means now

Two objectives have been re-weighted this session and each moved the compromise
without raising the ceiling. The live levers are the coordinate (Blocks A–C) and
the size-time law (Block E), and both may return negative. **Improving the z = 2
numbers by fitting them harder is not improvement** — C18 showed that dependence
is the selection, exp61 showed per-epoch fits overshoot the population, and
exp63's own per-epoch sequence breaks at z = 2. A third negative should be stated
plainly as a class result rather than dressed as progress.

## Branch state at end of session

- **`exp73-size-relative-objective`** in the main worktree, level with
  `origin/master` at `5d142f4`, clean.
- `master` at `5d142f4` in `hongshao_master`, pushed.
- Other worktrees: exp50, exp51, exp55 (prunable), internal-review.

---

# Appended 2026-09-02 — the exp73 session, run continuously as planned

Branch **`exp73-size-relative-objective`**, ~17 commits ahead of `master`
(`5d142f4`), **NOT merged, NOT pushed**. Read
`experiments/exp73_size_relative/README.md` first; every block's numbers are
there.

## What happened, in one paragraph each

**A1 — the coordinate.** Built (`coordinate.py`, eight selftest claims). Its
usable window is 0.5–6 R50 **only because the user pointed out that the isophote
density reaches 0.673 kpc for every galaxy at every epoch**; on the stored 2 kpc
grid nothing below 1 R50 is usable at z = 2 and the loss is mass-dependent. The
density-rebuilt curve is spliced inside 2 kpc; `cog_provided` is untouched
outside (D1). **It corrected exp72**: the "self-similar in R50 units" claim was
half a coverage artefact — outside 1 R50 the galaxy is self-similar (3.0–3.7
points), inside it is not (9.3 points of real evolution). Fixed inline in exp72
and C21.

**A2 — the risk test.** A per-galaxy metric cannot see the joint-vs-per-epoch gap
at all; the binned tercile-median gate can, and under it the cost of sharing one
θ falls 0.28 → 0.07 (relative) on R50 shells. **Gate A was written ambiguously**
(ratio vs excess) and resolved by the plan's own rule. The bigger caveat: the
per-epoch comparators were optimised on fixed kpc, so the R50 "ceiling" is not
a true one — **five single-epoch refits under the R50 objective are owed**
(~75 min). The per-epoch size optima below z = 2 lie within 3–6 per cent of one
power of (1+z): **D3's lever is small; Block E was evaluated and not fitted.**

**B — the weighting sweep (D2).** Scheme B disqualified by the pre-stated rule
(136 / 215 per cent variation with `t`); A barely helps; the mass-weighted
residual wins on allocation and usability. **Two galaxies carry 78 per cent of
the z = 2 loss on fixed kpc** — the user's claim 2, confirmed.

**D — the size gate (D1).** `hongshao.qa` gained R20 and `size_gate` (offset
≤ 0.05 dex; width within 20 per cent), at fixed stellar and halo mass. First use
on three known models: it catches the χ² refit's 27-per-cent-too-big on OFFSET
at exactly the right epochs, exp63 passes 15/15 on the merged grid, and **width
fails 0/15 for every mean model, collapsing with redshift** (exp63 R50 0.63 →
0.30, R80 0.16 at z = 2) — C16 made quantitative. The two sub-gates are counted
separately for that reason.

**C — the refit: a negative, my design error, and a recorded one.**
`SizeGrid.shells()` was a bare `np.diff` over 0.5–6 R50 — no inner aperture —
so the objective was blind to a third of the galaxy's mass, and the fit put
+100 per cent inside 2 kpc for a 0.2 per cent loss change. Under its own
objective it beats exp63 by 23 per cent; under exp63's it is **eleven times
worse**, R50 a quarter too small at every epoch. Memory
`density-objective-cannot-see-the-centre` records exactly this mechanism and its
fix. The coordinate is repaired (aperture as bin 0, selftest claim H) and **the
fit was NOT re-run** — that is the user's call. Blocks A and B stand; exp63's
θ already scored 21 per cent better than the incumbent under this objective
before any fitting.

## Decisions owed by the user

1. **Refit under the repaired objective?** ~1.5 h, four starts, `fit_re.py` as
   is (the coordinate fix is already in). The judge `eval_re.py` is ready.
2. Five single-epoch R50 refits for A2's true ceiling (~75 min).
3. Whether the stochastic layer is re-scored on the new size gate.
4. C19 — untouched, deliberately.
5. Merge.

## Lessons the session left (all in `doc/lessons.md`)

A coverage-limited statistic reports on whoever survived; ask what else the
data product holds before concluding it cannot answer; count sub-gates
separately; a gate on an extrapolated quantity is weaker; write a gate in the
units it will be read in; **a recorded lesson is only as good as the check that
it is consulted** — assert "does mass inside the first edge change the
residual?" before the first fit under any differenced objective; and a fit that
wins its own objective and loses everyone else's by 10× has found a blind spot.

## 2026-09-02 afternoon — the user chose decision 1; the repaired refit is a null result

The user asked whether the alarming exp73 QA figures were the objective's blind
spot and whether the refit would fix them: yes on both. Refit launched 12:45
(nohup, main shell, four starts, 75 min), judged 14:05–14:20. **Block C's blind
outputs are preserved as `outputs/*_blockC_blind.*`.**

- **The blind-spot failure is gone**: inside 2 kpc the refit is +7 per cent at
  z = 0.4 where the blind fit was +102; R50 is −1 / 0 / −1 / +6 / +11 per cent
  where it was −22 to −26.
- **The refit is exp63 within about 10 per cent on every parameter.** Best start
  is exp63's, 7.34 against exp63's own 8.00 under this objective (−8 per cent),
  and **+5.1 per cent worse under exp63's loss** (16.10 vs 15.33). Size-gate
  offset 14/15 for both; R50 at z = 2 +11 per cent against exp63's +5; 1 kpc at
  z = 0.4 +46 against +35 per cent; total mass at 148 kpc slightly better at
  z = 0.4 and z = 2. A dial on the shared-law compromise — C20's result again.
- **The positive statement is about exp63**: an objective that sees sizes
  directly returns exp63's parameters, its size-time exponents included. Block
  E (richer size-time law) is now doubly unmotivated.
- The three far starts stopped at the 3000-evaluation cap 12–15 per cent above
  the best; the basin is not proven global. Not worth more evaluations: the
  judge says the basin's best is not an improvement.
- `qa._size_figure` crashed on the fourth size fraction (`zip` silently dropped
  R90's style); fixed with a length assertion and re-run.

Decisions 2–5 are unchanged and still the user's. My recommendation on 2 is now
weaker: with the R50 objective shown to be a dial, five single-epoch R50 refits
would measure a ceiling of the same class as exp61's, which does not transfer.

## 2026-09-02 evening — decision 2 taken: five single-epoch R50 refits; A2's headline corrected

`fit_re.py --epoch k` (freeze as exp63's per-epoch fits; five starts incl. exp63's
own per-epoch optimum) ran for all five epochs in parallel (16:00–17:10, 0.4 GB
each while fitting — the 11.5 GB is a transient at build); `ceiling_re.py` judged
(`outputs/ceiling_re.log`). README "Block A2, closed".

- **True cost of sharing under the R50 objective: +20 per cent at every epoch
  (1.17–1.23), against +28 per cent under fixed kpc.** A2's 1.07–1.14 was the
  mis-specialised comparator. The coordinate removes about a third of the cost
  of sharing, not three-quarters. A2's headline is corrected in the README.
- **The per-epoch ceiling overshoots the other way**: R50 per-epoch fits make
  galaxies 3–6 per cent too small and 3–5 per cent too heavy inside 10 kpc
  (exp63's: 3–7 per cent too big); under exp63's loss they are 10–28 per cent
  worse at their own epoch. Not a target, for a second objective.
- **One real finding**: the z = 2 break in the extended deposit size that exp63
  Stage 5i found under fixed kpc (−0.49 → −0.16) is absent under R50 (−0.62 →
  −0.57): it was the fixed-kpc weighting of z = 2's empty outskirts (C21), not
  the population. The compact size runs as one power of (1+z), exponent −0.67
  (exp63 joint −0.80): D3 stays small.
- The qa stellar-mass-bin footnote is one line now (the user: it broke the
  layout).

Still owed: decision 3 (stochastic layer on the size gate), C19, merge.

## 2026-09-02 night — figures for the per-epoch fits; decision 3 (the layer on the size gate)

- `ceiling_figures.py`: standard battery for each `R50 e{k}` +
  `figures/exp73_ceiling_own_epoch.*` (3 terciles × 5 epochs, own-epoch fits vs
  the joint fits on the merged grid). The cost of sharing is a 13–18 per cent
  dip at 2 kpc in the most massive haloes at z ≥ 1.5 that the own-epoch fits
  hold to 3–5.
- `size_gate_layer.py` (decision 3): hongshao v1's Option A layer (around the
  INCUMBENT mean) scored on the tier 2d gate, 8 draws. **Width passes 3 of 15**
  (R50 at z ≤ 1.0; 1.14 / 0.99 / 0.87 / 0.76 / 0.73 across z). **R20
  over-dispersed 2.3× at z = 0.4; R80 under-dispersed by half everywhere.** The
  truth's size diversity grows outward, the layer's is concentrated in the
  core — the missing piece is an OUTER size component (exp60 Option C's 2-D
  size axis), and the core draw is too wide. README "Block D, second use".
- Ops: a foreground Bash call is capped at 10 min; the first layer run was
  killed at draw 6/8 — anything over ~5 min goes through nohup.

Still owed: C19; merge; and whether exp63's mean gets a layer (the v1 layer is
built around the incumbent, whose high-z R50 offsets fail the gate that exp63
passes).
