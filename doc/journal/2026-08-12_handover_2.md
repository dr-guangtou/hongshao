# Session Handover — 2026-08-12 (session 2: exp45 + two new QA tiers)

## Goal
exp45 is COMPLETE (all four follow-ups run). The user's NEXT request is a
**DISCUSSION, not a build: they want to understand the "high-z ridge"
before acting on it.** Start there.

## Completed This Session
Branch `exp45-coherence-scoreboard`, **6 commits, UNMERGED** (the user
did not ask for a merge this time — ask before merging).

1. **exp45 — the coherence scoreboard** (`scoreboard.py`): 13 recorded
   thetas + both layer modes + the exp37 emulator's saved draws scored on
   tier 2c; NO new fitting. **The over-coherence defect is UNIVERSAL to
   the kernel family** — eleven deterministic variants across both
   channel counts and all five fit scopes are indistinguishable (excess
   +0.49 to +0.66, energy 8.7-12.3x floor). Two partial exceptions land
   at the SAME +0.45 (inner-aware objective, 1ch only; real-MAH input),
   both previously rejected. **The exp37 emulator does not have the
   defect at all** (-0.16, 2.1x) — it has an AR(1) latent by design.
   -> the exp44 `ar1` trade is a LIVE decision, not settled.
2. **Follow-up 1** (`followups.py sweep`): only `sig` (the efficiency
   window width) moves coherence (+0.96 -> +0.67 over 0.24 -> 1.20).
   **q is COMPLETELY INERT**, so is log_rc — coherence is set by WHEN
   stars arrive, not by transport. Saturates at +0.67 vs truth +0.33.
3. **Follow-up 2** (`followups.py emulator_physics`): exp37's draws
   UNDERSHOOT the differential test at every pair (0.32/0.09 vs data
   0.37/0.11 flagship; 0.15/0.04 vs 0.23/0.06 highest; kernel 0.40/0.13
   and 0.21/0.06). The two-model division of labour SURVIVES.
4. **`qa` tier 2c graduated** (`growth_planes`, figure `qa_growth_*`)
   and **tier 2d** (`size_planes`, figure `qa_size_*`) — the MASS-SIZE
   planes, which had never been scored despite being a named success
   criterion. `enclosed_radius(cog, R, frac)` generalizes
   `half_mass_radius`; tier 2d reports **R50 / R80 / R90**. Both tiers
   take the model's radii from the MODEL CoG (the one documented
   exception to the truth-shared convention). Both are in `evaluate()`,
   self-checked, and documented in `doc/tech_note/README.md`; the
   adopted product's reference QA set was regenerated with them.
5. **User hypothesis CONFIRMED — the size offset IS the inner deficit.**
   Per-galaxy Spearman between dlog R50 and the M(<5 kpc) bias is
   **-0.89 to -0.97 at EVERY epoch**.

## In Progress (Not Finished)
- **NOTHING building. The next item is a CONVERSATION.** The user:
  "let's discuss the high-z ridge task, I still need to understand it
  better." I offered three framings and recommended the second:
  (a) what "ridge" means concretely and how exp41 established the
  residual is the MEAN model's, not the layer's; (b) **why it appears
  specifically at z >= 1.5** — the record has genuinely competing,
  never-separated explanations (dissipative-era assumption failure per
  exp40, the fit scope, or the deposit machinery); (c) what would fix
  it. Recommend (b).
- **New material for that discussion, found today:** the high-z size
  story is **TWO effects**. At z <= 1.5 the R50 offset is the known
  inner deficit re-expressed (R80/R90 near zero). **At z = 2.0 the
  ordering FLIPS** (R80/R90 +0.116/+0.112 vs R50 +0.087) — a genuine,
  SEPARATE outer over-extension. Note z=2.0 is the pure-extrapolation
  epoch under the adopted z<=1.5 scope, which is suggestive but
  untested.

## Problems / Blockers
- None. Watch-outs unchanged: `export
  HONGSHAO_DATA_DIR=/Users/shuang/Desktop/tng300_mah_mprof`; soft
  hyphens (U+00AD, check with perl not BSD grep -P); `/bin/rm` in
  scripts; exp39-45 scripts path-load exp38 stage2/3 (spawn-safe
  `_w_init`); the exp42/exp45 real-MAH rows need `config="real"`.

## Key Decisions
- The user's framing, accepted: the models are deterministic with a
  clear functional form, so intrinsic size-evolution coherence is
  unsurprising, and it does not matter for the model's PURPOSE
  (single-epoch population distributions) — which our own measurement
  supports, since single-epoch planes are provably rho-blind. The
  consequence recorded: by that criterion the top outstanding problem
  is the z >= 1.5 plane residual, and the mass-size plane is now the
  weakest scored deliverable (E/floor 3.1 at best, 7.8 at z=2, vs
  1.0-1.1 on the mass planes).
- Tier 2d reports THREE radii, not one, because R50 alone conflates an
  inner-mass defect with a size error.
- Documented + self-checked interpretation caveat: **R90 AMPLIFIES** —
  the CoG is flat there, so an inner-ONLY deficit biasing R50 by +0.048
  dex still drags R90 by +0.042. Read the ORDERING, never the gap size.

## Branch State
- Branch: `exp45-coherence-scoreboard`, clean, **6 commits ahead of
  master, NOT merged, NOT pushed**.
- Untracked, deliberate: `doc/CLAUDE.md` (NEVER commit) and this
  handover (commit as the next branch's first commit, or on this one).
- Gitignored: exp45 `outputs/*.npz` + `figures/*`.

## Files Modified This Session
- New: `experiments/exp45_coherence_scoreboard/{README.md,
  scoreboard.py, followups.py}`,
  `doc/journal/2026-08-12_handover_2.md` (this file).
- Updated: `hongshao/qa.py` (tier 2c already there; +`enclosed_radius`,
  `size_planes`, `_size_figure`, tier-2d reporting, SIZE_FRACTIONS,
  self-checks), `doc/tech_note/README.md`, `doc/todo.md`, wensai journal
  `hongshao_exp45-coherence-scoreboard_2026-08-12.md`.

---
*Session: 0a9faee5-d7e7-433b-a6ae-a040aa790cc2 — resume with
`claude --resume 0a9faee5-d7e7-433b-a6ae-a040aa790cc2`*
