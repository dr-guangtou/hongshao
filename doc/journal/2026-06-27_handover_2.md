# Session Handover — 2026-06-27 (#2, exp27 + exp28)

## Goal
Turn the exp25 deposition-kernel toy into a physical model. This session gathered the
TNG data the model needs: cross-matched the sample to the official DiffMAH/DiffStar
(exp27) and built/compared every MAH flavour from the merger trees (exp28).
**Next session: return to the deposition kernel — use a MAH with no artificial
dipping, and replace the centred-Gaussian primitive with something better.**

## Completed This Session
- **exp27 — TNG-API cross-match → official DiffMAH/DiffStar** (merged `d7a1540`).
  Bridge = snap-72 position (diffmah array column == snapshot number; exact).
  **3154/3388 matched** (234 merge/disrupt by z=0). Reconciled own vs official
  DiffMAH (curves agree 0.078 dex after fixing a **Msun/h** unit bug);
  `diffmah_combined.fits` with official-default/own-fallback. h-free masses are now
  the repo default (`AGENTS.md`). Paper-verified DiffMAH = main-branch Mpeak.
- **exp28 — MAH flavours from the SubLink tree** (merged `ae7e922`). One reusable
  `mah_flavours(tree)` → `main_raw/main_mpeak/max_prog/max_prog_mpeak/summed/
  infall_peak/main_m200c`, compared to DiffMAH on 2 example halos.
  `experiments/exp28_summed_accreted_mah/{mah_flavours,viz_tree}.py`.
  - **`max_prog_mpeak`** = defect-robust central halo (recovers main-branch
    dropouts) → **the recommended dip-free MAH for the kernel**.
  - `summed` over-counts at high z (other galaxies' halos); `infall_peak` is the
    monotonic ex-situ delivery clock (merger-accreted-only, excludes ~40% smooth).
  - DiffMAH was fit to **SubhaloMass** (not M200c); its *fit* has a ~0.1–0.17 dex
    residual at z=0.4. Lit review: `M_sum(z)` is not a standard named quantity
    (`doc/summed_accreted_mah.md`). Tree format: `doc/tng_merger_trees.md`.
- **Full-tree fetch campaign** — `scripts/fetch_full_trees.py` (resumable, chunked,
  most-massive-first). Cost measured: ~30–100 s/tree, **serial only** (concurrency
  503s), ~2–3 days for all 3154; disk ~40 GB kept / ~0 streamed; bulk = 1.5 TB.
  **Chunk 1 fetched: 11/3154 done, 1.3 GB** in `/Users/mac/work/tng/full_trees/`.

## In Progress (Not Finished) — the NEXT task
**Return to the deposition kernel (exp25/26 line) with a better primitive.**
1. **Choose the dip-free MAH.** Recommend **`max_prog_mpeak`** (central-halo,
   defect-robust); `infall_peak` is the alternative (delivery clock). Only 11 halos
   have full trees so far → either fetch more chunks, prototype on the available
   examples, or use the smooth **official DiffMAH curve** for the population step.
2. **New deposition primitive — better than a centred Gaussian.** exp26 showed the
   *added* stellar mass grows **inside-out & multiplicatively** (`Σ_low/Σ_high ∝
   R^b`, long-baseline **b≈+0.95**; outer 60 kpc grows ~29× vs inner 8 kpc ~4×; only
   ~3% core-drop). Try a **multiplicative power-law amplification / outer-weighted
   (shell) deposit** instead of a centred Gaussian. (See exp26 README + figures.)
3. Tie the kernel's time axis to the dip-free MAH from step 1.

## Problems / Blockers
- Full trees exist for only 11/3154 (rest = parked multi-day campaign). Resume with
  `scripts/fetch_full_trees.py --next N`. Big clusters (logMh≳14.8) are 503-prone.
- The dip-free tree MAHs (`max_prog_mpeak`, `infall_peak`) are only available where we
  have full trees; the official DiffMAH curve (all 3154) is the dip-free fallback for
  population work, at the cost of its ~0.1 dex fit residual.

## Key Decisions
- **h-free masses (Msun) are the repo default** (`AGENTS.md`); ingest external
  catalogs (DiffMAH is Msun/h) with +0.169 dex.
- Full-tree pulls run as **resumable chunks, stream-friendly** — never bulk-download
  (1.5 TB) and never assume concurrency (serial only).
- Compare DiffMAH fits by **reconstructed curve**, not raw params (degenerate).

## Branch State
- Branch: **master** (exp27 + exp28 merged & pushed; `origin/master` at `ae7e922`).
- Uncommitted: only this handover note + the untracked `doc/CLAUDE.md` (claude-mem
  artifact — do not commit) + MEMORY.md update.
- No open feature branch — start the next session by branching for the kernel work.

## Files Modified This Session
- exp27: `experiments/exp27_tng_api_crossmatch/{fetch_mpb,crossmatch,qa,diffmah_consistency}.py`, `README.md`
- exp28: `experiments/exp28_summed_accreted_mah/{mah_flavours,viz_tree}.py`, `README.md`
- utility: `scripts/fetch_full_trees.py`
- docs: `doc/{summed_accreted_mah,tng_merger_trees,mah_definitions_research,lessons,todo}.md`, `AGENTS.md`, this handover.

---
*Session: 87a94bcd-5af5-4af9-a18d-80bcddb33c93 — resume with `claude --resume 87a94bcd-5af5-4af9-a18d-80bcddb33c93`*
