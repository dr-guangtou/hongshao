# Session Handover — 2026-08-28

*(Supersedes `2026-08-28_exp63_handover.md`, written earlier the same session; this
one carries the user's end-of-session reading and direction.)*

## Goal

The user asked for a fresh-eyes pivot on the deposition-only model: (1) prove the
"follow the halo mass increments, then sum" recipe is an analytic integral; (2) a new
single-epoch (z=0.4) Direction-3 model that grows a galaxy along its MAH and explains
the data better without unphysical transport; (3) a model that is statistical from
the start. **The user's standing direction for the next session: focus on the z=0.4
epoch ONLY, judge by the average curves of growth in halo- and stellar-mass bins,
and explore every reasonable way to improve the single-epoch performance —
particularly the highest halo-mass bin and the outskirts, which degrade against the
incumbent.**

## Completed This Session (branch `exp63-analytic-growth`, 27 commits ahead of `master` at `a43d823`, NOT pushed, nothing merged)

- Science plan `doc/plans/2026-08-28-exp63-analytic-growth-model.md` (user-approved;
  decisions D1–D5 in §4) and per-stage implementation plans
  `doc/plans/2026-08-28-exp63-stage{0,1,2,3}-implementation.md`.
- **Tech note 04** `doc/tech_note/04_analytic_deposition_integral.md` (+ figures 04, 04b,
  scripts `04_analytic_check.py`, `04b_sersic_vs_deposition.py`): the exact integral;
  the convolution-in-log-radius form; the incomplete-gamma closed form (the Sérsic
  *integral* over deposit size, not the Sérsic profile — figure 04b).
- `experiments/exp63_analytic_growth/`: `engine.py` (exact integral, 0.32 s per
  full-sample 5-epoch evaluation, bridge to `exp54/model.forward` at 2.4e-6 dex),
  `stage0_engine.py` + `stage0_rebaseline.py` (gates pass; D3 → truncation kept;
  Result 0: the incumbent's shape defect is not numerical), `stage1_deconvolve.py`
  (bimodal deposit-size distribution; n=4 rejected; c200c no leverage; G1 two
  scales), `model2.py` (two-channel mean, nested bit-for-bit; options `tau_d`,
  `g_split`, `g_rel`), `stage2_fit.py` (z=0.4-ONLY fit, gates, extrapolations,
  figures incl. the C8 split; `--delay`, `--tau-fixed`, `--growth`, `--growth-rel`),
  `process3.py` (three in-history noise sources, Campbell-checked),
  `stage3_process.py` (sweep, held-out fit, gates, extrapolations),
  `stage4_battery.py` (battery on three products; tier 2e from draws). README with
  every stage and a closing summary; PROBLEMS.md P1–P10.
- Key numbers, all in the README: Stage 2 (12 params, fitted at z=0.4 only, one
  optimum from 7 starts): inner slope 1.018 vs data 1.015, M(<10) −1.9 %, tier 3
  0.268 (incumbent 0.280) — but the binned average CoGs show the top halo-mass
  tercile −12 % at 2–4 kpc and the outskirts not better than the incumbent; the
  extrapolation to z ≥ 0.7 shows a global tilt and is worse than the (five-epoch)
  incumbent in amplitude. Stage 3 process: held-out kpc-plane E/floor 1.20–1.28
  (mean alone 2.6–2.9; exp60 1.69–2.17), tier-2e widths from draws 0.94–1.02 at z=0.4
  (C16 closed) and 0.95–1.09 at every extrapolated epoch, persistence +0.68.
- Rejected on the fly (recorded in README + P9): accretion-to-deposition delay;
  absolute and relative growth-rate splits (right assembly-correlation signs, broken
  extrapolation).
- Records: `doc/todo.md`, `doc/lessons.md` (incl. the user's end-of-session
  reading), memory (`exp63-results`, `deposit-size-distribution-is-bimodal`,
  `deposition-sum-is-an-analytic-integral`, `halo-history-knows-a-little-about-the-core`,
  `exp63-analytic-growth-plan`), Obsidian journal
  `~/work/wensai/wensai/development/hongshao/hongshao_exp63-analytic-growth_2026-08-28.md`.

## In Progress (Not Finished)

- Nothing running; nothing uncommitted. The next work is a NEW round, not a
  continuation: **improve the z=0.4 single-epoch mean, judged by the binned average
  CoGs.** Concrete first steps, in order:
  1. Refit Stage 2 with the time exponents FROZEN at the incumbent's values (a_z,
     a_Mz, b_c, b_e) — a single epoch cannot constrain them and the optimiser used
     them to tilt the extrapolation. Judge by `qa_bins`/`qa_bins_ms` at z=0.4 first.
  2. Target the highest halo-mass tercile (−12 % at 2–4 kpc) and the outskirts:
     candidates — a halo-mass-dependent extended size (the Stage 1 extended mode
     moves 43→74 kpc across terciles), a compact share that does not fall so far at
     high mass, an outskirt-aware weighting in the objective (the production loss is
     blind beyond ~50 kpc — memory `the-loss-is-blind-past-its-own-pin`), the extended
     kernel's shape.
  3. Only after z=0.4 is right by the visual gate, look at the extrapolation.

## Problems / Blockers

- Decisions owed by the user: merge/push the branch; P10 (mean- vs median-preserving
  noise convention); whether exp63's mean + process replaces exp60's layer; whether
  P9 is worth pursuing (nothing simple works).
- The obsidian CLI (`obsidian vault=wensai ...`) hung for 2 min this session; the
  vault path is `~/work/wensai/wensai` (a stale, near-empty `~/Dropbox/wensai` also
  exists — do not write there).

## Key Decisions

- D1 analytic R200c; D2 no c200c; D3 truncation kept; D4 production loss + shells/log;
  D5 both samples at every predicted epoch (user, interview).
- The Stage 2 mass-only split is the exp63 mean; the delay and growth-rate splits are
  rejected by the extrapolated epochs (agent, on the fly, recorded).
- The user's verdict on Stage 2 by the visual gate: not a clear z=0.4 improvement;
  worse at the extrapolated epochs (global tilt). Lessons recorded.

## Branch State

- Branch: `exp63-analytic-growth`, 27 commits ahead of `master` (`a43d823`), never
  pushed. Uncommitted changes: none (this file and the memory update are the last
  writes). No background processes.

## Files Modified This Session

New: `doc/tech_note/04_analytic_deposition_integral.md`, `doc/tech_note/scripts/04*.py`,
`doc/tech_note/figures/04*`, `doc/plans/2026-08-28-exp63-*.md` (5 files),
`experiments/exp63_analytic_growth/{engine,model2,process3,stage0_engine,stage0_rebaseline,
stage1_deconvolve,stage2_fit,stage3_process,stage4_battery}.py`, its `README.md`,
`PROBLEMS.md`, `doc/journal/2026-08-28_exp63_handover.md`, this file. Modified:
`doc/tech_note/README.md`, `doc/todo.md`, `doc/lessons.md`. Memory: five files + index.

---
*Session: e198b694-406b-4106-9a10-d3f7d269a541 — resume with `claude --resume e198b694-406b-4106-9a10-d3f7d269a541`*
